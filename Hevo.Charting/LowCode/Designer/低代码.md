# 低代码蓝图设计

> 由 JSON 蓝图在运行时动态孵化 `ReactiveSchema` 的能力。
> 实现位于 `Hevo.Charting/LowCode/Designer/`,由 `ChartBlueprint` + `DynamicChartSchema<T>` + `ComponentRegistry` + `SmartActivator` + `ComponentMetadataRegistry` + `BlueprintRunner` 六件套构成。
>
> **工程改造计划**见下文 [第二部分](#第二部分工程改造优化方案):反射热路径缓存、类型冲突早期诊断、单例污染兜底、JsonConverter schema 稳定化等带优先级的落地清单(§B1–§B11 + §K 全部交付)。
> **JSON 序列化标准入口**:[BlueprintJsonOptions.Default](Converters/BlueprintJsonOptions.cs) ——
> 蓝图持久化要走这份 options(注册了 Color / IHevoBrush 多态 Converter + NaN/Infinity 数字策略),
> 别每个调用点 new 一份新的,否则 schema 不对齐 → round-trip 出问题。

---

## 1. 蓝图模型(JSON Schema)

`ChartBlueprint` 的运行态结构(见 [ChartBlueprint.cs](ChartBlueprint.cs)):

```jsonc
{
  // 1. 数据源:类型名 + 字段→引脚 ID 的映射
  "DataSource": {
    "TypeName": "TimeShareDataSource",
    "ScalarMappings": {                  // 标量字段(单值上下文,从 DataSource 实例上反射取值)
      "PreClose": "Env_PreClose"
    },
    "VectorMappings": {                  // 向量字段(从 TItem 上反射取列,自动包成 ReadOnlyMemory<T>)
      "Time":  "RealTime_Time",
      "Price": "RealTime_Price"
    }
  },

  // 2. 初始 Trait 播种:Schema 启动时一次性 Seed 进 Shared 域
  "InitialTraits": [
    {
      "TraitTypeName": "ScaleStrategyTrait",
      "Preset": "Default"                // 直接拿 ScaleStrategyTrait.Default 静态字段
    }
  ],

  // 3. Features 装配清单:运行时反射创建并 Add 到 canvas
  "Features": [
    {
      "TypeName": "PlotAreaDecorFeature",
      "Properties": { /* 普通可写属性 */ },
      "PortBindings": { /* Feature 上 DataPort<T> 字段 → 全局引脚 ID */ }
    },
    {
      "TypeName": "LineSeriesFeature",
      "Properties": { "LayerName": "TrendLine" },
      "PortBindings": {
        "DataPort":   "RealTime_Price",
        "YRangePort": "Final_Y_Range"
      }
    }
  ]
}
```

> ⚠️ 历史上曾设计过 `Layers` / `ComputeNodes` / `Sinks` 三段式蓝图,代码已演进为**只有 Features**(见 [ChartBlueprint.cs:11](ChartBlueprint.cs:11) 注释)。文档与代码同口径。

---

## 2. 装配引擎

```csharp
// 业务侧一行启动
var ds = new TimeShareDataSource();
BlueprintRunner.Run<TimeData>(host.Cell, blueprint, ds, ds.Stream);
// 等价快捷重载(自动接 ds.Stream)
BlueprintRunner.Run(host.Cell, blueprint, ds);
```

`BlueprintRunner` 内部:

```csharp
var schema = new DynamicChartSchema<TItem>(blueprint, dataSource, stream);
chart.Template = schema; // 由 ChartCell 接管 schema 生命周期
```

`DynamicChartSchema<T>` 三件事(见 [ChartBlueprint.cs](ChartBlueprint.cs)):

1. **`DefineDataFlow`**
   - 优先查 [`ComponentMetadataRegistry`](ComponentMetadataRegistry.cs):若数据源类型登记了自定义策略,整支管线交由策略接管。
   - 否则按 `DataSource.ScalarMappings` 反射构造 `ContextIngestor<TItem,TSource,TValue>`、按 `VectorMappings` 反射构造 `ScatterIngestor<TItem,TValue>`。
   - 用 `StartWith(ds.GetSnapshot())` 灌首帧,`DoOnDispose(pipe.Dispose)` 收尾,最后 `.BindTo(chart)` 注册主流。
   - 数据源若实现 `IDisposable` / `IPausable`,会自动通过 `RegisterDisposable` 挂在 schema 生命周期上,无需业务 `OwnedBy(this)`。
2. **`DefineFeatures`** — 遍历 `InitialTraits` Seed,遍历 `Features`:
   - `ComponentRegistry.Resolve(TypeName)` 拿 Type
   - `Activator.CreateInstance` 实例化 (要求 Feature 有 public 无参 ctor;有参的需走自定义注册或 `IPipelinePolicy`)
   - `SmartActivator.InjectProperties` 注 Properties
   - `PortBindings` 把全局引脚焊到 Feature 的 `DataPort<T>` 字段(init-only 字段反射 SetValue 直接穿透)
   - `canvas.Add(feature)`
3. **全局引脚注册表** — 同一个 `portId` 在该 Schema 内全局唯一,跨 Feature 共享 `DataPort<T>` 实例。

---

## 3. 组件注册

### 手动登记
```csharp
// 应用启动时注册可用类型,Resolve 时按 TypeName 字符串查表
ComponentRegistry.Register<LineSeriesFeature>();
ComponentRegistry.Register<TimeShareDataSource>();
ComponentRegistry.Register<ScaleStrategyTrait>();
```

### 反射批量登记(推荐)
```csharp
// 自动收录 Hevo.Charting 自带的 ChartFeature / IVisualTrait / DataSource
BuiltinRegistration.RegisterBuiltins();

// 顺手把业务程序集也扫一遍 (TimeShareDataSource 等)
BuiltinRegistration.RegisterAssemblyOf<TimeShareDataSource>();
```

收录条件(详见 [BuiltinRegistration.cs](BuiltinRegistration.cs)):
- ChartFeature:public、非抽象、非泛型定义、有 public 无参 ctor (例:`AxisFeature` 因 ctor 需 `ITickProvider` 不会被自动登记,如需暴露给蓝图,业务侧自行 wrap 一个无参派生类)。
- IVisualTrait:public、非抽象、非泛型定义。无参 ctor 不强制 (record 走 Preset)。
- DataSource:沿继承链找 `DataSource<,>`。

未注册的 `TypeName` 在 Resolve 时直接抛"未知组件类型"异常,蓝图加载即失败。

---

## 4. 性能优化代理(透明优化)

### 矛盾

图形引擎为 60 FPS 百万级渲染,对特定业务流(如 A 股分时图 240 点)采用**预分配数组占位**(`BindIndexedColumn`)。这种内存优化绝不能反向污染 JSON 蓝图,否则低代码用户得理解列式存储。

### 解药:`ComponentMetadataRegistry`

蓝图编译期发现某 `DataSource` 类型在元数据表里登记了 `IPipelinePolicy`,编译器自动接管该数据源的管线装配,**蓝图层零感知**。

```csharp
// 业务侧自定义策略
public class TimeShareIndexedPolicy : PipelinePolicy<TimeShareDataSource, TimeData>
{
    protected override void CompileTyped(
        ChartBlueprint blueprint,
        TimeShareDataSource ds,
        UniversalDataPipe<TimeData> pipe,
        Func<Type, string, object> getOrCreatePort)
    {
        // 这里走预分配数组占位的高性能装配路径,而非通用反射
        // ...
    }
}

// 启动时登记
ComponentMetadataRegistry.Register<TimeShareDataSource>(new TimeShareIndexedPolicy());
```

策略命中后,`DynamicChartSchema` 的通用反射路径被完全绕开,蓝图 JSON 不需要任何变化。

### 戒律

- ❌ **禁止**在蓝图模型(JSON)里暴露性能相关参数(如 `IsFixedLength`)
- ✅ **必须**在 `ComponentMetadataRegistry` 里登记 DataSource 的管线编译策略,保持蓝图模型纯粹

---

## 5. SmartActivator 物化策略

[`SmartActivator.MaterializeTrait`](ComponentRegistry.cs):

| StyleModel 配置 | 行为 |
|---|---|
| 仅 `Properties` | 走 `Activator.CreateInstance(type)` + `InjectProperties` (传统 POCO) |
| 仅 `Preset` | 取 `type` 上 public static 字段/属性,直接复用单例(record / 位置参数 trait 适用) |
| `Preset` + `Properties` | 取预设实例后再覆盖部分字段(注意:可能污染共享单例,业务自负) |

`InjectProperties` 已支持 `JsonElement` 降级、Enum 字符串、`Nullable<T>`、`TypeConverter`、`IConvertible` 五重兜底,JSON 常见坑点全覆盖。

---

## 6. 当前局限

- `ComponentRegistry` 不区分作用域,JSON 中所有 `TypeName` 共享同一张全局表;多业务线复用蓝图时建议用 alias 区分。
- 含 ctor 复杂参数的 Feature(`AxisFeature(ITickProvider)` 已经在框架层用无参 ctor + init 属性铺平,见 §7;真要换 `ITickProvider` 实现仍需业务代码侧 new)。
- 闭包式参数(`Func<int>` / `Func<int, DateTime>` / `Action<...>` 等)JSON 表达不出来,典型例 `ScheduleTickProvider`、`OnRequireDataAsync` 仍需业务侧 wrapper 写死。

---

## 7. 可视化拖拽编辑器 (GraphViewer) — 已落地

> 实现位于 [`Hevo.Charting/LowCode/Designer/GraphViewer/`](GraphViewer/),业务侧入口
> [`GraphViewerBootstrap.Initialize()`](GraphViewer/GraphViewerBootstrap.cs)。
> Window4 "低代码" Tab 是参考集成。

### 7.1 架构概览

GraphViewer 是 `ChartSchema` 的子类(**非 ReactiveSchema**),纯像素坐标系,5 层渲染管线:

| 层 | Z 槽位 | 职责 |
|---|---|---|
| `GraphCanvasLayer` | Background | 底色 + 自适应网格 + **类别泳道**(按节点 Y 范围动态合并出 6 条横向带子) |
| `GraphEdgeLayer` | Main | 贝塞尔连线(采样为 polyline,跨帧拖拽不会击穿渲染端的 SVG cache) |
| `GraphNodeLayer` | Main | 圆角矩形 + 标题栏 + 端口圆点(扇入端口画方块,字尾 `[]`) |
| `GraphSelectionLayer` | Selection | 选中描边 + 框选橡皮筋 |
| `GraphPreviewLayer` | Interaction | 拖出连线橡皮筋 + 端口悬停 tooltip(name / type / 注释) |

5 层共享同一个 `GraphRenderTrait(GraphState)` 实例,放各自 local bag —— `GraphState` 引用换新一发,`VisualDependencyTracker.CheckIfDirty` ReferenceEquals 一击命中,所有 layer 同帧 MarkDirty 重画。

### 7.2 数据模型

```csharp
record GraphState(
    IReadOnlyList<Node>  Nodes,
    IReadOnlyList<Edge>  Edges,
    IReadOnlySet<string> SelectedNodeIds,
    CanvasTransform      Transform,
    EdgeRubberBand?      RubberBand,
    BoxSelection?        BoxSelection,
    HoveredPort?         HoveredPort
);

record Node(string Id, string TypeName, string Title, NodeKind Kind, HevoPoint Position,
            HevoPoint Size, IReadOnlyList<Port> InputPorts, IReadOnlyList<Port> OutputPorts,
            Dictionary<string, object?> Properties, FeatureCategory Category);

record Port(string Id, string Name, string DataTypeName, bool IsInput,
            string? Description, bool IsArray);

record Edge(string Id, string FromNodeId, string FromPortId, string ToNodeId, string ToPortId);
```

任何变更都走 `state with { ... }` 出新引用;[`GraphState.AddEdge`](GraphViewer/GraphState.cs) 对扇入端口允许多 incoming,普通端口覆盖。

### 7.3 反射驱动的元数据

| 来源 | 用途 | 文件 |
|---|---|---|
| `[PortDirection(Output)]` | 框架自有 feature 直接标方向(`UniversalAutoScale.YRangePort` / `ChartInteraction.PointerHitPort` 等) | [PortAttribute.cs](../PortAttribute.cs) |
| `PortMetadataRegistry` | 第三方 / 不便改源码的类型,运行时覆盖方向 + 注释 | [PortMetadataRegistry.cs](GraphViewer/PortMetadataRegistry.cs) |
| `FeatureCategoryRegistry` | 类型 → 6 类(DataSource / Trait / Environment / Axes / Series / Interaction) | [FeatureCategoryRegistry.cs](GraphViewer/FeatureCategoryRegistry.cs) |
| `[Description("...")]` | 标准 `System.ComponentModel` 属性,作端口 hover tooltip 第三行的来源之一 | (业务自由贴) |

`NodeFactory.CreateNode(typeof(Foo))` 一次性把这些元信息查好,baked 进 `Node` 实例;
渲染 / hover / 编辑器都读这份 baked 数据,跨帧零反射。

### 7.4 蓝图协议扩展

[`ChartBlueprint.cs:DefineFeatures`](ChartBlueprint.cs) 现支持两种 port 形态:

| 形态 | JSON | 后端解析 |
|---|---|---|
| 单 `DataPort<T>` | `"DataPort": "global_price_id"` | `GetOrCreatePort` + 反射 SetValue |
| `DataPort<T>[]` 扇入 | `"ValuePorts": "id1,id2,id3"` (CSV) | 拆 CSV → 逐个 GetOrCreatePort → 反射写数组 |

GraphViewer 侧 [`GraphSerializer.ToBlueprint`](GraphViewer/GraphSerializer.cs):
- 检测 `Port.IsArray`,扇入端口所有 incoming edges 用 `string.Join(",", ids)` 编码
- **每个 Feature 自己的 OUTPUT 端口也写 PortBindings 字段**(关键 fix —— 否则像 `UniversalAutoScale.YRangePort` 这种 init=null 的写出端口字段一直是 null,Watch 回调 NRE 静默吞,axis/grid/line 全黑)
- DataSource 节点 `Properties` 字典除 `ScalarMappings/VectorMappings` 外的字段进 `bp.DataSource.Properties`,运行时 `SmartActivator.InjectProperties` 注入(支持配 `MinuteCeiling` 等 init 标量)

#### 7.4.1 端口类型严格化(防御性 GetOrCreatePort)

[`DynamicChartSchema.GetOrCreatePort`](ChartBlueprint.cs) 升级成类型严格版,签名 `object?`:

- 同一 portId 已被以另一个 T 注册过 → 返回 null + warning,不返回脏类型让 ctor 抛 ArgumentException
- 三处调用方(`CompileScalarMappings` / `CompileVectorMappings` / `DefineFeatures.PortBindings`)统一 null-check + skip
- `IPipelinePolicy` 自定义 pipeline 路径走 `?? throw`(类型对策略是已知的,冲突算 bug 显式暴露)
- `ScatterIngestor` 反射 ctor 改用 `ConstructorInfo.Invoke`(`Activator.CreateInstance` 对 `null` arg 会做类型推断,跟 `Func<TItem,int>?` 形参签名匹配失败抛 `MissingMethodException`)

GraphViewer 侧 `TryWire` (sample helper) 也加了 `Port.DataTypeName` 字符串相等校验,蓝图里类型不匹配的连线在拉的时候就拒掉,不污染 portRegistry。

#### 7.4.2 Viewport 节点 — schema 顶层 ViewportPorts 的可视化代理

`Viewport.LogicalLength` / `UserRange` / `ActiveRange` 是 schema 内部 ports,但**蓝图必须显式连**:`DataSource.LogicalLength → Viewport.LogicalLength` 这根线缺了 → ViewportManager 永远看到 length=0 → ActiveRange invalid → axis/series 早退黑屏。

为此引入 [`NodeKind.Viewport`](GraphViewer/GraphState.cs) + [`NodeFactory.CreateViewportNode`](GraphViewer/NodeFactory.cs):

- Viewport 节点不对应任何 .NET 类型,GraphSerializer 检测 edge 末端是 Viewport → globalId 强制走 well-known id (`VP_LogicalLength` / `VP_UserRange` / `VP_ActiveRange`)
- DataSource ScalarMappings 同步覆盖,ingestor 直接写到 schema 顶层 port
- `DynamicChartSchema` 构造时 `_portRegistry["VP_LogicalLength"] = this.Viewport.LogicalLength` 等预登记 → 任何使用 well-known id 的端口拿到的就是 schema 自己持有的 ViewportPorts 实例,**不创建副本** → 数据流通

LinkedDashboard 模式将来自动复用:`SchemaContext.AttachedProperty` 注入外部 `ViewportPorts`,`this.Viewport` 自动是注入的实例,同一行预登记代码自动指向共享端口,蓝图层零感知。

### 7.5 框架"小幅低侵入"改造

为了让蓝图直接用框架原生 feature 而无须 wrapper,做了**通用化改进**(不引入低代码思想):

- [`AxisFeature`](../../Features/AxisFeature.cs):公共无参 ctor + `Format` / `Name` / `TickProvider` init 属性;`_tickProvider` / `_axisLayer` / `_gridLayer` 移到 `OnCompose` 延迟物化。`AxisStyleTrait` 仍作整体 record 保留,由编辑器递归当配置组编辑(见 §7.7)。
- [`LineSeriesFeature`](../../Features/LineSeriesFeature.cs):`Meta` 改 nullable,加 `MetaName` / `MetaColor` / `MetaFormat` 标量配置兜底,跟 `BarSeriesFeature.Meta` 默认值对齐。
- [`UniversalAutoScaleFeature`](../../Features/UniversalAutoScaleFeature.cs) / [`ChartInteractionFeature`](../../Features/ChartInteractionFeature.cs):输出端口标 `[PortDirection(Output)]`。

剩下需要 wrapper 的只有**关闭泛型**这一类:[`CrosshairDoubleFeature : CrosshairFeature<double>`](GraphViewer/Wrappers/BlueprintFeatureWrappers.cs)、`TooltipDoubleWidgetFeature` —— 反射在 UI 层闭合泛型代价大于一行子类。

### 7.6 类别泳道与编辑器分区

跟 [`FeatureCanvasExtensions`](../../FeatureCanvasExtensions.cs) 4 类 builder 一一对应,加上 DataSource / Trait 共 6 个 `FeatureCategory`:

| Category | 来源 | 颜色 | 含哪些 feature |
|---|---|---|---|
| **DataSource** | 蓝图概念 | 蓝 #4FC3F7 | TimeShareDataSource / KlineDataSource / ... |
| **Trait** | 蓝图概念 | 紫 #CE93D8 | ScaleStrategyTrait / GridStyleTrait / ... |
| **Environment** | `EnvironmentBuilder` | 灰青 #90A4AE | GridLayout / Viewport / AutoScale / PlotAreaDecor / UniversalHeader / DataPaging |
| **Axes** | `AxesBuilder` | 橙 #FFB74D | AxisFeature |
| **Series** | `SeriesBuilder` | 绿 #66BB6A | LineSeries / BarSeries / Candle |
| **Interaction** | `InteractionBuilder` | 红 #EF5350 | ChartInteraction / Crosshair / Tooltip / ConsoleDebugger |

落地三处:
1. **画布泳道**:[`GraphCanvasLayer.ComputeCategoryBands`](GraphViewer/GraphLayers.cs) 根据当前节点 Y 范围动态聚合,**用户拖动节点泳道实时跟随**;空 category 不渲染。
2. **Picker 分组**:[`ComponentPickerWindow`](GraphViewer/ComponentPickerWindow.cs) 按 category 分段显示,组头彩色加粗。
3. **默认样本**:Window4 初始画布按 category 横向排版(DataSource → Environment → Axes → Series → Interaction)。

业务自定义 feature 接入只需:
```csharp
BuiltinRegistration.RegisterAssemblyOf<MyFeature>();
FeatureCategoryRegistry.Register<MyFeature>(FeatureCategory.Series);
PortMetadataRegistry.RegisterOutputs<MyFeature>("OutPort1");   // 可选
```

ScaleStrategyTrait 这种"天然属于环境"的 trait 在 [GraphViewerBootstrap](GraphViewer/GraphViewerBootstrap.cs) 里登记成 `FeatureCategory.Environment`(覆盖默认的 Trait),节点跟 GridLayout / Viewport / AutoScale 同泳道排排坐。

### 7.7 节点属性编辑器

[`NodeEditorWindow`](GraphViewer/NodeEditorWindow.cs) — 双击节点弹出,反射枚举可写标量属性:

| 类型 | 控件 |
|---|---|
| `bool` | CheckBox |
| `enum` | ComboBox |
| `Color` | 文本(`#RRGGBB` / 颜色名)+ 实时色块预览 |
| `ChartLength` | 单位 ComboBox(Pixel/Star/Auto) + 数值 TextBox |
| `IHevoBrush` | 模式下拉(None / Solid / Resource):Solid 走 Color 编辑 / Resource 写资源 Key |
| `string` / 数值 / `DateTime` 等 | TextBox + `TypeConverter` 兜底 |
| **record / 含主构造的 DTO** | **递归展开为"配置组"**,通过主构造函数重新装配(`AxisStyleTrait` / `LineStyle` / `FieldMeta` 等无需特化) |

Mapping / VectorMappings(DataSource)单独走字典编辑器,每行 `字段名 → portId`。

### 7.8 BlueprintLauncher — 运行时上下文注入

[`BlueprintLauncher.Launch`](GraphViewer/BlueprintLauncher.cs) 签名:
```csharp
public static string? Launch(
    ChartBlueprint blueprint,
    Window? owner = null,
    object? dataSourceContext = null,                  // 路 A:类型化便捷
    Action<object>? configureDataSource = null);       // 路 B:万能回调
```

**两条互补的路注入数据源运行时上下文**(蓝图模型不携带的部分):

- **路 A**:`dataSourceContext` 类型化参数。Launcher 反射查 DataSource 上是否有 `LoadAsync(TContext)`,参数类型可赋值 → 自动 fire-and-forget 调用。`ReactiveDataSource<TSource, TContext, TItem>` 派生类全部命中。Window4 工具栏的"证券" TextBox 用这条路:用户填代码 → `Security.FromString` → `dataSourceContext`。
- **路 B**:`configureDataSource` 万能回调,LoadAsync 后同步执行,业务可自由处理(MinuteCeiling 配置、多步 Load 链、自定义 SwitchContext 等)。

| 类型 | 路 A | 路 B | 蓝图 Properties |
|---|---|---|---|
| `Security` 这种"运行时身份" | ✅ 类型化参数 | ✅ 回调 | ❌ 不该入蓝图 |
| `MinuteCeiling = true` 这种"产品化偏好" | ❌ | ✅ 回调可选 | ✅ DataSource.Properties 持久化 |
| 复杂多步 init | ❌ | ✅ 唯一选择 | ❌ |

### 7.9 Compute 节点抽象(为后续业务扩展铺路)

**蓝图层统一观点**:画布上任何"转换节点"都是 input port → 黑盒 → output port,底层用什么实现是细节:

| 节点性质 | 输入源 | 输出 | 现有底层 | 何时用 |
|---|---|---|---|---|
| **Source Ingestor** (Scalar/Vector) | `DataSnapshot<TItem>` | `DataPort<T>` | `ContextIngestor` / `ScatterIngestor` | snapshot 驱动,装在 `UniversalDataPipe` 链上 |
| **Compute Feature** | `DataPort<T>` | `DataPort<T>` | 派生 [`ComputeNodeFeature`](GraphViewer/Wrappers/LinkWrappers.cs) | board → board 纯计算 |
| **Side-effect Compute** | 多个 `DataPort<T>` | 多个 `DataPort<T>` | `ChartFeature.OnCompose` + `flow.Watch` | 写入链同步触发 (典型 UniversalAutoScale) |

⚠️ **Source Ingestor 不能等同 Compute Feature**:`ChartFeature.OnProject` 体系里只能读 board,看不到 `DataSnapshot<TItem>`。所以源端摄入必须装在 `UniversalDataPipe.Ingestor` 链上,蓝图侧通过 `DataSource.ScalarMappings/VectorMappings` 字段表达(已有协议)。Board → Board 转换才用 `ComputeNodeFeature`。

[`ComputeNodeFeature`](GraphViewer/Wrappers/LinkWrappers.cs) 模板:
```csharp
public abstract class ComputeNodeFeature : ChartFeature
{
    public override FeaturePhase Phase => (FeaturePhase)50;   // Layout 之后、Series 之前

    // 子类自己声明:
    //   public DataPort<T> InputPort { get; init; } = null!;
    //   [PortDirection(PortDirection.Output)]
    //   public DataPort<T> OutputPort { get; init; } = null!;

    protected sealed override void OnProject(FeatureContext ctx) => OnComputeProject(ctx);
    protected abstract void OnComputeProject(FeatureContext ctx);    // 读 input → 算 → 写 output
}
```

业务派子类(例 MovingAverage、PercentChange、Indicator 等)→ 注册 → picker 自动收录、画布自动可视化、NodeEditorWindow 自动认参数。**框架不动一行**。

### 7.10 Link 家族(传统 .Pipe() DSL 蓝图化对应)

[`Wrappers/LinkWrappers.cs`](GraphViewer/Wrappers/LinkWrappers.cs) 文件头标注 DSL ↔ 蓝图机制对应:

| 原 DSL | 蓝图机制 |
|---|---|
| `.Inject(ds => ds.PreClose).ForwardTo(P)` | `DataSource.ScalarMappings` + edge |
| `.Inject(ds => ds.LogicalLength).ForwardTo(VP.LogicalLength)` | 画布拉一根线到 Viewport 节点(§7.4.2) |
| `.LinkStream(cfg => cfg.AutoMap())` | `DataSource.VectorMappings` (auto) |
| `.Compute<S>(s, action)` | 派 `ComputeNodeFeature`(§7.9) |

剩下不能直接表达的:
- `Func<TIn, TOut>` 转换 lambda — JSON 写不出,只能业务派子类硬编码
- `Func<int>` / `Func<int, DateTime>` 等闭包参数(`ScheduleTickProvider`、`OnRequireDataAsync`)— 同上

### 7.11 已知约束(设计期保留)

- **节点视觉自定义不能用 DataTemplate**:Layer 渲染是 `DrawOp` 指令缓冲。要每种节点长得不一样,要么手写 `DrawOp` 分支,要么走 `WidgetBuffer` 塞 UserControl。
- **trait 判脏粒度是引用级**:改 GraphState 必须 `with { ... }`,**不能原地 mutate**;否则 `VisualDependencyTracker.CheckIfDirty` 走的 `ReferenceEquals` 命中失败,UI 不重画。
- **连线类型校验**走 `Port.DataTypeName` 字符串相等(`"object"` 通配),拖拽时高亮"可连/不可连"。严格 .NET 类型语义校验未来再补。
- **Source Ingestor 必须有 ds.LogicalLength → Viewport.LogicalLength**:这根线断 → ViewportManager 看到 length=0 → 全部下游 IsValid=false 早退黑屏。默认 sample 已预连。

### 7.12 待补 (生产级体验)

- ✅ ~~撤销栈 / Redo~~ — [`GraphSchema._undoStack/_redoStack`](GraphViewer/GraphSchema.cs) 实现。模型变更前打 snapshot 入栈(view-only 操作如 pan/zoom/选中不入),Ctrl+Z 弹回 + 当前态推 redo,Ctrl+Y / Ctrl+Shift+Z 反向。栈深 100。Undo/Redo 还原模型时保留当前 view (Transform),不把视口拉回旧位置。Window4 的"加节点 / 改属性 / 清空"统一走 [`ApplyUserEdit`](GraphViewer/GraphSchema.cs) 入栈;拖拽 / 删除 / 接线在 schema 内部 `CommitDrag` / `PushUndoAndApply` 收口。
- ✅ ~~节点对齐线(拖拽时显示)~~ — MoveNodes 拖动时,`ComputeAlignmentSnap` 对每个被拖节点的 left/centerX/right + top/centerY/bottom 与"未被拖"节点的同三档位求最近距离,≤6px(画布坐标)就吸附 + 收一根贯穿屏幕的辅助线;`AlignmentGuides` 进 GraphState,`GraphPreviewLayer` 用青色虚线渲染。落点/丢失捕获时清空。
- ✅ ~~minimap 缩略图~~ — [`GraphMinimapLayer`](GraphViewer/GraphLayers.cs) 落 Interaction 槽位,右下角 200×140 浮窗:节点缩略 + 当前视口框(亮黄)。鼠标在 minimap 内点击/拖动 → `_isMinimapDrag = true` → `CenterViewportOnMinimapClick` 把对应画布点搬到 chart 中心,`PanCanvas` 路径里走 `TryHandleMinimapDrag` 跟随,松开复位。
- ✅ ~~反序列化既有 `ChartBlueprint` JSON → 还原成 GraphState~~ — 已在 [`GraphDeserializer.FromBlueprint`](GraphViewer/GraphDeserializer.cs) 实现:节点经 `NodeFactory.CreateNode` 反射重建,`PortBindings` 全局 globalId 反查回 (FromNodeId, FromPortId) 拉边,VP_* well-known id 自动孵化 Viewport 节点;位置由 `AutoLayout` 按 Category 分列、按入度纵向排开。
- `PortBindings` 数组协议接入更多场景(`CrosshairFeature.YTrackers` 等)
- LinkedChartDashboard 编辑器:多 cell 工作区,共享 `SchemaContext`(viewport 端口已就绪,缺工作区/dashboard 上层概念)

---

## 8. 关键 fix 历史(本次落地的踩坑记录)

> 这些是把蓝图从"画布看着挺像"做到"运行起来真能渲染"必须修的硬骨头,记一下避免重蹈。

1. **GraphSerializer 漏写 OUTPUT 端口绑定**:`UniversalAutoScale.YRangePort` 的 init 默认值是 `null!`,蓝图必须显式焊一个 DataPort 实例。Serializer 之前只给 INPUT 边写 binding,导致 OUTPUT 字段一直是 null,Watch 回调 NRE 静默吞,axis/grid/line 全黑。Fix:每个 Feature 序列化时,自家 OutputPorts 也写 `PortBindings` 字段,跟下游 input 共享同一全局 portId(同 `GetOrCreatePort` 缓存)。
2. **DynamicChartSchema 漏写 Viewport.LogicalLength**:每个传统 schema 都 `.ProjectExtent(Viewport)` 把 `ds.LogicalLength` 写到 `Viewport.LogicalLength`,缺它则 ViewportManager 看到 length=0 → 写 `default(RealRange)` → 全部下游早退黑屏。后来升级成 Viewport 节点 + well-known id,蓝图侧画一根线显式表达。
3. **ScatterIngestor ctor 类型推断失败**:`Activator.CreateInstance(Type, params object[])` 对 `null` 参数(`indexSelector`)无法反推 `Func<TItem,int>?` 形参签名,抛 `MissingMethodException`。Fix 改用 `ConstructorInfo.Invoke` 直接按位置塞参数。
4. **端口类型 cache 冲突**:同一 portId 被两个不同 T 的端口请求(例 Time:DateTime 错连到 *:double),`GetOrCreatePort` 之前返回缓存的脏类型,ctor 抛 ArgumentException 把整张图带崩。Fix:类型严格化,签名改 `object?`,冲突返回 null + warning,调用方 skip;`TryWire` (sample) 也加 `DataTypeName` 校验。
5. **Edge 渲染同 source 多线视觉重叠**:同一 OutputPort 派出多条边时,贝塞尔起点重合 → 看起来像一根。Fix:`GraphEdgeLayer.OnUpdate` 维护 `(fromNodeId, fromPortId) → 已绘次数` 计数,第 N(≥1) 条线纵向偏移 ±8*N px(交替正负),曲线分散开。同时 `DrawBezier` 控制点逻辑加强:反向边(b.X &lt; a.X)的水平 dx 拉到 `max(120, dy*0.6+dx*0.5)` 防 S 太紧穿点;陡边(|dy| &gt; |dx|*1.5)的 dx 拉大避免视觉直角。
6. **Edge 被中间 node 遮挡 → 数据流可见性断**:画布上 edge 起止节点之间常常路过别的 node,默认贝塞尔直行被遮成两段。Fix 三件套:
   - `GraphEdgeLayer` 移到 `GraphNodeLayer` **之上**(同 Main 槽位,后加),数据流连续可见性优于节点视觉完整性(参 Houdini / n8n / Unreal Blueprint 惯例)。
   - `GraphEdgeLayer.ComputeAvoidanceVOffset` 扫所有非端点节点,如果 AABB 横向跟边重叠且节点中心 Y 与边在该 X 处的预期 Y 距离 ≤ `halfH+16px` 安全垫,把控制点 Y 推开,曲线腹部绕到节点上方/下方。多障碍取推力最大方向,限幅 ±180 px。
   - 双笔渲染:深色半透明 outline (3px) 在底层兜底"过浅色节点头部也能看见",上层亮色 EdgePen (1.5px) 表达连接关系本身。
7. **AutoLayout 列分组 → 类别横幅全重叠**:旧版按 Category 分**列**,每列从 y=60 起头独立排开 → `GraphCanvasLayer.ComputeCategoryBands` 按节点 Y 范围聚合横幅,所有 category 都覆盖 y=60 起的范围 → 视觉上横幅完全重叠,UX 报废。Fix:改成按 Category **横向泳道**,每泳道独占自己的 Y 行,内部按入度+barycentric 双模式优化:单节点泳道(DataSource/Axes/Series)自由 X = 邻居平均(让"主线"上下游对齐成竖直线);多节点泳道(Environment/Interaction)整体平移对齐外部连接重心,内部 indeg 顺序不动。迭代 5 轮收敛。详见 [`GraphDeserializer.AutoLayout.Apply`](GraphViewer/GraphDeserializer.cs)。
8. **连线像折线(贝塞尔采样成 polyline)**:`GraphEdgeLayer.DrawBezier` 把三次贝塞尔手动采样成 18 段 polyline 后走 `IDrawingSink.DrawPolyline`,长边上每段 50px+ 的直线段视觉上能看到折角,曲线像锯齿。Fix:加 `DrawOp.DrawCubicBezier` 原生贝塞尔指令([Primitives.cs](../../Renderers/Primitives.cs)),WPF 端用 `StreamGeometryContext.BezierTo` 走 GPU 抗锯齿曲线管线([DrawingRenderer.BuildCubicBezier](../../Renderers/DrawingRenderer.cs)),任何长度都丝滑;DrawBezier 直接调 `draw.DrawCubicBezier(pen, p0, c1, c2, p3)` 不再手算采样。
9. **AutoLayout 反向边导致"S 钩"弯折**:旧版按 indeg 或 barycentric 排,孤立配置节点(decor/grid/vpManager 这种没 Port 也没 edge 的)占住 col 0-3,把真正的连接节点 scale 顶到 col 4;但 line/yAxis(scale 的下游消费者,在另一泳道)的 col 是 2 → 边 (scale, line) 从 col 4 反向连到 col 2,贝塞尔回钩成 S。Fix:[`ComputeDepth`](GraphViewer/GraphDeserializer.cs) 算每个节点的 DAG 深度(Kahn 拓扑序最长路径),泳道内排序按 (是否有 edge → depth → alphabetic),孤立节点推到泳道末尾;col 派位用 `max(node.depth, lastColInLane + 1)` 强制 col ≥ 深度。任何 edge (a→b) 天然 a.col < b.col,完全消除反向。
10. **JSON 预览/导入对 NaN/Infinity 炸**:业务数据源经常带 `double.NaN` 这种"程序员默认值"(典型 [TimeShareDataSource.PreClose](../../../Hevo.Drawing/TimeShare/TimeShareDataSource.cs)),NodeEditorWindow 反射读出来塞进 Properties 字典 → `JsonSerializer.Serialize` 默认行为是 `InvalidOperationException("infinity cannot be written as valid JSON")`。Fix 两件套:
    - Window4 共享一份 `JsonSerializerOptions` 加 `NumberHandling = AllowNamedFloatingPointLiterals`,导出/导入两端复用,NaN 写成 `"NaN"` 字符串(System.Text.Json 标准扩展)。
    - NodeEditorWindow `MakeEditorByType` 的 TextBox 路径加 `IsSpecialFloat` 判断,NaN/Infinity 一律按空串显示;用户留空 → CoerceTextValue 返回 null → SmartActivator.SafeChangeType 写非 nullable double 失败被静默吞,目标对象保持原默认值。

---

# 第二部分:工程改造优化方案

> 配套上文蓝图设计的工程改造计划。每项条目按"触发条件 + 现状 + 方案 + 验证 + 工作量"格式落地,可单条独立实施,也可按波次推进。
> 涉及的反射热路径量化数据来自 [LowCodeDemoView.xaml.cs](../../../Hevo.Drawing.LowCodeDemo/LowCodeDemoView.xaml.cs) 的 sample 蓝图(13 节点)+ TimeShare 业务蓝图(约 50 节点)实测。
>
> **第一波 §B1–§B6 已落地**:实测数据与回归测试详见 [Hevo.Charting.Benchmarks/README.md](../../../Hevo.Charting.Benchmarks/README.md) 与 [Hevo.Charting.Tests](../../../Hevo.Charting.Tests)。

---

## B0. TL;DR

| # | 名称 | 优先级 | 实测收益 | 状态 |
|---|---|---|---|---|
| B1 | 端口元数据缓存 | P0 | 50 节点 CreateNode 15μs 总计 (~300ns/节点,反射开销消失) | ✅ 落地 |
| B2 | Activator 替换为编译委托 | P0 | .NET 8 上微基准持平/略慢(Activator 已优化);保留为代码一致性 | ✅ 落地 |
| B3 | 属性 setter 编译化 | P0 | **2.1× faster + 0 alloc** (121ns + 40B → 58ns + 0B) | ✅ 落地 |
| B4 | `Seed<T>` MakeGenericMethod 缓存 | P0 | **15× faster + 0 alloc** (589ns + 232B → 38ns + 0B) | ✅ 落地 |
| B5 | 类型冲突早期诊断 | P1 | DryRun 50 Feature 121μs;黑屏调试分钟→秒 | ✅ 落地 |
| B6 | Preset+Properties 单例兜底 | P1 | 消除一类隐性 bug | ✅ 落地 |
| B7 | 复杂值类型 JsonConverter | P2 | Color/IHevoBrush schema 稳定 + 单元测 round-trip 通过 | ✅ 落地 |
| B8 | PortBindings 数组化 | P2 | 扇入端口 JSON array form;CSV 老格式仍读 | ✅ 落地 |
| B9 | Benchmark 项目 | P3 | 性能回归可测 | ✅ 落地 |
| B10 | 蓝图往返一致性单测 | P3 | 序列化正确性,36 测试全绿 | ✅ 落地 |
| B11 | ComponentRegistry 作用域 | P3 | 多业务线复用 alias prefix 隔离 + 旧蓝图回退兼容 | ✅ 落地 |
| **K** | **Trigger 协议 + Handler 注册表 + AutoDiscover (K线 迁移驱动)** | **P0** | **业务带 trigger / 闭包的 schema 也能蓝图化** | **✅ 落地** |

> **第一波 (§B1–§B6 + §B9 §B10) 已交付**:[Hevo.Charting.Benchmarks](../../../Hevo.Charting.Benchmarks) + [Hevo.Charting.Tests](../../../Hevo.Charting.Tests)
> **第二波 (§B7 §B8) 已交付**:[Converters/](Converters/) 含 BlueprintJsonOptions / ColorJsonConverter / HevoBrushJsonConverter,LowCodeDemoView 已切换到统一 options。
> **第三波验证**:嵌套 record (LineStyle / AxisStyleTrait / HevoPen) 经 §B7 框架 round-trip 通过,
> 不需为每个 trait 单独写 converter ——顶层多态 + 叶子值类型 converter + record 默认反序列化已足。
> **第四波 (§K) 已交付**:[BlueprintHandlerRegistry](BlueprintHandlerRegistry.cs) + [TriggerModel](ChartBlueprint.cs) +
> [\[BlueprintHandler\] attribute + AutoDiscover](BlueprintHandlerRegistry.cs);K线 业务侧迁移
> 范例 [Hevo.Drawing/Kline/KLineBlueprintModule.cs](../../../Hevo.Drawing/Kline/KLineBlueprintModule.cs)(原 KLineSchema.cs 保留)。
> **第五波 (§B11) 已交付**:[BuiltinRegistration.RegisterAssemblyOf<T>(prefix)](BuiltinRegistration.cs) + [ComponentRegistry.Resolve 前缀回退](ComponentRegistry.cs) +
> 单测覆盖 [ComponentRegistryScopeTests](../../../Hevo.Charting.Tests/ComponentRegistryScopeTests.cs)。
> **当前状态**:测试全绿,benchmark 量化数据见 [Hevo.Charting.Benchmarks/README.md](../../../Hevo.Charting.Benchmarks/README.md)。

---

## B1. 端口元数据缓存

**触发条件**:恒做,编辑器新建/拖动节点的反射开销在 100+ 节点工作区肉眼可感。

**现状**:[NodeFactory.CreateNode](GraphViewer/NodeFactory.cs) 每次都 `GetProperties` + `[PortDirection]` 扫描 + `PortMetadataRegistry` 查询,无缓存。
- 单次 ~0.5ms(DataPort 字段多的 Feature 翻倍)
- 编辑器画 100 节点 = 100 次全扫
- Picker 列出候选时还会再触发一遍(候选项 preview)

**方案**:

```csharp
internal static class NodePortCache
{
    // 💥 终身缓存:type 是 RuntimeType,GC 行为跟程序集生命周期一致,不会泄漏到内存悬挂
    private static readonly ConcurrentDictionary<Type, NodePortShape> _shapes = new();

    public static NodePortShape Get(Type t) =>
        _shapes.GetOrAdd(t, ScanShape);

    private static NodePortShape ScanShape(Type t)
    {
        // 把 NodeFactory.ScanFeaturePorts 当前的反射逻辑搬过来
        // 输出:Port[] inputs, Port[] outputs, FeatureCategory category
    }
}

// NodeFactory.CreateNode 改成读 NodePortCache.Get(type)
```

注意 [GraphViewerBootstrap.Initialize](GraphViewer/GraphViewerBootstrap.cs) 末尾批量预热一次(遍历 ComponentRegistry.ListAll → Get),把首屏新建节点也压平。

---

## B2. Activator 替换为编译委托

**触发条件**:恒做,蓝图加载是 launcher 的关键路径。

**现状**:

- [SmartActivator.MaterializeTrait](ComponentRegistry.cs) + [BlueprintLauncher](GraphViewer/BlueprintLauncher.cs) + [DynamicChartSchema.DefineFeatures](ChartBlueprint.cs) 全部走 `Activator.CreateInstance`
- 50 Feature 蓝图 = 50 次反射 ctor,~30ms
- 同 type 反复创建(多个 LineSeriesFeature 实例)也不复用

**方案**:在 ComponentRegistry 内挂 `Type → Func<object>` 编译缓存:

```csharp
private static readonly ConcurrentDictionary<Type, Func<object>> _ctorCache = new();

public static object CreateInstance(Type t) =>
    _ctorCache.GetOrAdd(t, BuildCtor)();

private static Func<object> BuildCtor(Type t)
{
    var ctor = t.GetConstructor(Type.EmptyTypes);
    if (ctor == null)
    {
        // 💥 fallback:有些 record / 含必要 init 的类型用 ConstructorInfo.Invoke 兜底
        // 见已知坑 上文 §7.4 ScatterIngestor null arg 推断失败
        return () => Activator.CreateInstance(t)!;
    }
    // Expression.New 编译后的委托等价于直接 newobj IL,无反射开销
    return Expression.Lambda<Func<object>>(Expression.New(ctor)).Compile();
}
```

调用点替换:

| 文件 | 位置 | 替换 |
|---|---|---|
| [SmartActivator.MaterializeTrait](ComponentRegistry.cs) | 默认无 Preset 路径 | `ComponentRegistry.CreateInstance(t)` |
| [BlueprintLauncher.cs](GraphViewer/BlueprintLauncher.cs) | DataSource 实例化 | 同上 |
| [ChartBlueprint.cs](ChartBlueprint.cs) | DefineFeatures Feature 实例化 | 同上 |

**.NET 5 兼容**:`System.Linq.Expressions` 在 BCL 内,无新依赖。Roslyn 不涉及。

---

## B3. 属性 setter 编译化

**触发条件**:跟 §B2 配套做,单独做意义不大。

**现状**:[SmartActivator.InjectProperties:45](ComponentRegistry.cs:45) 每个属性 `PropertyInfo.SetValue`:

- struct trait([`HevoPoint` / `LineStyle`](../../Globals.cs))的属性注入会 box → SetValue → unbox,每次新分配
- 同 (Type, propName) 反复查询 `GetProperty`

**方案**:

```csharp
private static readonly ConcurrentDictionary<(Type, string), Action<object, object?>?> _setterCache = new();

private static Action<object, object?>? GetSetter(Type t, string propName) =>
    _setterCache.GetOrAdd((t, propName), key =>
    {
        var pi = key.Item1.GetProperty(key.Item2);
        if (pi == null || !pi.CanWrite) return null;

        // 💥 init-only 属性:.NET 5+ 反射 SetValue 直接穿透。Expression.Assign 同样穿透,
        //    因为 IL 层面 init 只是 modreq(IsExternalInit),CLR 不强制
        var instParam = Expression.Parameter(typeof(object), "instance");
        var valParam  = Expression.Parameter(typeof(object), "value");
        var castInst  = pi.DeclaringType!.IsValueType
            ? Expression.Unbox(instParam, pi.DeclaringType)  // struct 走 unbox(返回 ref),写穿透到 box
            : Expression.Convert(instParam, pi.DeclaringType);
        var castVal   = Expression.Convert(valParam, pi.PropertyType);
        var assign    = Expression.Assign(Expression.Property(castInst, pi), castVal);

        return Expression.Lambda<Action<object, object?>>(assign, instParam, valParam).Compile();
    });
```

⚠️ **struct 注入仍会 box**(`object` 参数本质是 boxed ref,无法绕开),但去掉了反射调度开销,吞吐提升来自后者。如果将来 struct trait 有热路径 setter,可特化出 `ref` 版本。

---

## B4. `Seed<T>` MakeGenericMethod 缓存

**触发条件**:InitialTraits 多于 5 个时累计开销可见。

**现状**:[ChartBlueprint.cs DefineFeatures](ChartBlueprint.cs) 处理 InitialTraits 时必须用 `MakeGenericMethod` 兜泛型推断坑(上文 §7.4 已记录此为已知约束):

```csharp
// 当前(伪代码)
var seedMethod = typeof(ReactiveSchema)
    .GetMethod(nameof(Seed))!
    .MakeGenericMethod(traitType);
seedMethod.Invoke(this, new[] { traitInstance });
```

每个 trait 都做一次 `MakeGenericMethod`(几十 μs)+ `MethodInfo.Invoke`(box arg)。

**方案**:缓存编译后的 `Action<ReactiveSchema, object>`:

```csharp
private static readonly ConcurrentDictionary<Type, Action<ReactiveSchema, object>> _seedCache = new();

private static Action<ReactiveSchema, object> GetSeeder(Type traitType) =>
    _seedCache.GetOrAdd(traitType, t =>
    {
        var schemaParam   = Expression.Parameter(typeof(ReactiveSchema), "s");
        var instParam     = Expression.Parameter(typeof(object), "trait");
        var seedGeneric   = typeof(ReactiveSchema).GetMethod(nameof(Seed))!.MakeGenericMethod(t);
        var call          = Expression.Call(schemaParam, seedGeneric, Expression.Convert(instParam, t));
        return Expression.Lambda<Action<ReactiveSchema, object>>(call, schemaParam, instParam).Compile();
    });

// 调用点
GetSeeder(traitType)(this, traitInstance);
```

---

## B5. 类型冲突早期诊断

**触发条件**:恒做,上文 §7.4.1 已经做了运行时严格化,但仍是"加载后才发现冲突"。

**现状**:[ChartBlueprint.cs:127](ChartBlueprint.cs:127)

- 端口类型不匹配只 `Console.WriteLine` 后 skip
- 蓝图能加载但 Watch 回调拿不到数据 → axis/grid/line 黑屏
- 业务侧排错只能靠"看日志 → 反推哪根线断了",调试时间分钟级

**方案**:在 [BlueprintLauncher.Launch](GraphViewer/BlueprintLauncher.cs) 入口加 dry-run 校验,把 launcher 返回值从 `string?` 升级成结构化结果。

```csharp
public sealed class BlueprintLaunchResult
{
    public string? Error { get; init; }                       // 致命错误,启动被中断
    public IReadOnlyList<BlueprintDiagnostic> Diagnostics { get; init; }
        = Array.Empty<BlueprintDiagnostic>();                 // 警告:启动了但有连接被 skip
    public bool Launched => Error == null;
}

public sealed class BlueprintDiagnostic
{
    public DiagnosticSeverity Severity { get; init; }   // Warning / Error
    public string Code { get; init; } = "";             // 例 "BP_PORT_TYPE_MISMATCH"
    public string FeatureTypeName { get; init; } = "";
    public string PortName { get; init; } = "";
    public string Message { get; init; } = "";
}
```

校验内容:

1. 所有 `PortBindings` 引用的 globalId 在该蓝图内首次注册时的类型 vs 后续引用类型,不匹配则报警告
2. Output 端口缺失 binding(上文 §8 关键 fix #1)→ 报警告
3. 引用了未注册 TypeName → 错误,中断启动
4. DataSource 找不到 `Stream` 属性 → 错误

⚠️ **不要拆破蓝图运行时已有的 silent skip**——校验是诊断层叠加,原有 skip 路径保留以防蓝图局部坏不影响其他 Feature。

**Window4 集成**:导入 JSON 后调一次 `BlueprintLauncher.DryRun(bp)`,结果显示在 jsonPreview 下方一个新 TextBlock;运行时也累积到这里。

**向后兼容**:保留旧 `Launch(...) → string?` 重载,调用 `LaunchExt(...).Error`,业务代码无需改。

---

## B6. Preset + Properties 单例污染兜底

**触发条件**:[ComponentRegistry.cs:95-107](ComponentRegistry.cs:95) + 上文 §5 已知风险。在 `Preset` 同时带 `Properties` 时,反射 SetValue 会写穿到静态单例,污染共享状态。

**现状**:仅有文档约定"业务自负",无运行时保护。两个不同蓝图共用同一 trait 类型时容易踩。

**方案**:

```csharp
// SmartActivator.MaterializeTrait
if (hasPreset && hasProperties)
{
    var preset = ResolveStaticPreset(t, model.Preset);
    // 💥 record 自动生成的 <Clone>$ 方法返回浅拷贝,init-only 字段也能在 with 表达式里改
    //    反射调用 <Clone>$ 等价于业务侧 `preset with { ... }`
    var cloneMethod = t.GetMethod("<Clone>$", BindingFlags.Instance | BindingFlags.NonPublic);
    var instance = cloneMethod != null
        ? cloneMethod.Invoke(preset, null)!
        : Activator.CreateInstance(t)!;  // 非 record 兜底:走默认 ctor + 后续 InjectProperties
    SmartActivator.InjectProperties(instance, model.Properties);
    return instance;
}
```

⚠️ **关键约束**:record 才有 `<Clone>$`。非 record trait(`class : IVisualTrait`)必须有公共无参 ctor 才能走 Properties 路径,否则降级成"用 preset 引用 + 警告日志"(保持原 behavior 不破坏现有蓝图)。在 §B5 的 dry-run 里把这种降级也报成警告。

---

## B7. 复杂值类型 JsonConverter ✅ 已落地

**触发条件**(已满足):
- LLM/MCP 开始生成蓝图,复杂值序列化形态成为 prompt context 输入
- 业务蓝图开始入版本仓做 diff 评审,发现 `IHevoBrush` / `LineStyle` 类型字段在 JSON 里散乱

**实际方案**(比原计划少做):

| 类型 | JSON 形态 | Converter |
|---|---|---|
| `Color` | `"#RRGGBB"` (alpha=255) / `"#AARRGGBB"` (含 alpha),读时容忍 `#RGB` / `#ARGB` / 无前缀 | [ColorJsonConverter](Converters/ColorJsonConverter.cs) |
| `IHevoBrush` | `{kind:"solid",color:"#..."}` / `{kind:"resource",key:"..."}` / `{kind:"linear",from,to,p0,p1}` | [HevoBrushJsonConverter](Converters/HevoBrushJsonConverter.cs) |
| `LineStyle` / `HevoPen` / `AxisStyleTrait` 等嵌套 record | System.Text.Json **默认 primary ctor 反序列化** + 上面两个叶子 converter 自动嵌套 | 不需要 |

**关键发现**:原计划准备给"3-5 个核心类型"各写 converter,实测发现**只需要写两个叶子 converter**:
- 顶层多态接口 `IHevoBrush` 一个 converter(用 `kind` 判别符)
- 叶子值类型 `Color` 一个 converter(hex 字符串)

中间 record (`HevoPen` / `LineStyle` / `AxisStyleTrait`) 走 System.Text.Json 默认行为,叶子 converter 自动嵌套生效。43 测试通过验证此结论。

**统一入口**:[BlueprintJsonOptions.Default](Converters/BlueprintJsonOptions.cs) —— 蓝图 JSON 全部走这一份 options(含 `WriteIndented` + `AllowNamedFloatingPointLiterals` + 上述两 converter)。LowCodeDemoView 已切换到这份。

**SmartActivator 适配**:[ComponentRegistry.cs SafeChangeType](ComponentRegistry.cs) 在 JsonElement 路径上:
1. 已注册 Converter 的类型 (Color / IHevoBrush):走 BlueprintJsonOptions 反序列化
2. JsonElement.Object / Array 形态:也走 Converter 反序列化(覆盖 LineStyle / AxisStyleTrait 等嵌套 record)
3. 其他 (String / Number / Bool):走原五重兜底

**关键坑**:`HevoBrushJsonConverter.CanConvert` 必须覆盖到所有 `IHevoBrush` 派生类——否则 `Dictionary<string, object?>` 序列化时按 runtime type (`HevoSolidBrush`) 找不到 converter,fallback 到默认 record POCO 形态,schema 错位。

**实际工作量**:~0.5d(远低于原计划 2d,因为不需要每个 trait 写 converter)

---

## B8. PortBindings 数组化 ✅ 已落地

**实际方案**:

模型类型从 `Dictionary<string, string>` 改成 `Dictionary<string, object?>`。引脚解析统一走 [PortBindingValue](PortBindingValue.cs) 辅助类,识别四种形态:

| 输入 | 来源 |
|---|---|
| `string` | 单端口 / `JsonElement.String` |
| `List<string>` / `string[]` | 业务侧手撸 / 新格式数组 |
| `JsonElement.Array` | 反序列化得到的 JSON 数组 |
| `"id1,id2,id3"` (CSV) | <v1 老蓝图兼容路径 |

```jsonc
"PortBindings": {
  "DataPort":   "RealTime_Price",                                  // 单端口仍 string
  "ValuePorts": ["RealTime_Price", "RealTime_Volume", "RealTime_Avg"]  // 数组端口写新形态
}
```

[GraphSerializer.ToBlueprint](GraphViewer/GraphSerializer.cs) 写出时单端口给 string、扇入端口给 `List<string>`,反序列化能读所有四种形态。

**实测**([PortBindingValueBenchmarks](../../../Hevo.Charting.Benchmarks/PortBindingValueBenchmarks.cs)):
- 老 CSV 5 ids: 610 ns + 568 B
- **新数组 5 ids: 388 ns + 336 B → 1.57× faster, 41% 少分配**
- 单 string: 5-30 ns + 0-32 B

**测试**:[BlueprintRoundTripTests](../../../Hevo.Charting.Tests/BlueprintRoundTripTests.cs) 新增 `PortBindings_ReadsArrayForm_NewFormat` + `PortBindings_ReadsCsvForm_LegacyCompat` 两条用例。

**实际工作量**:~0.5d

---

## B9. Benchmark 项目

**触发条件**:§B1-B4 落地后,需要量化收益证据。

**方案**:新建 `Hevo.Charting.Benchmarks/`(BenchmarkDotNet)。

⚠️ .NET 5 兼容线已删——benchmark 不构成生产代码,可独立 TFM。csproj 设 `<TargetFramework>net8.0</TargetFramework>` 即可,跟主项目解耦。

核心 case:

| Benchmark | 目标 |
|---|---|
| `LoadSampleBlueprint_13Nodes` | 测 [LowCodeDemoView.BuildSampleGraph](../../../Hevo.Drawing.LowCodeDemo/LowCodeDemoView.xaml.cs:172) → ToBlueprint → Launch 全链路 |
| `LoadTimeShareBlueprint_50Nodes` | 业务蓝图实测 |
| `Deserialize_BlueprintFromJson` | 纯反序列化 |
| `CreateNode_PerType` | 单节点反射构造 |
| `MaterializeTrait_PerType` | trait 物化 |

每个 case 加 `[MemoryDiagnoser]`,盯住 GC alloc——struct trait box 减少应有可见下降。

---

## B10. 蓝图往返一致性单测

**触发条件**:§B7-B8 schema 改造前必须落,否则改 Converter 不知道有没有破东西。

**方案**:新建 `Hevo.Charting.Tests/Blueprint/RoundTripTests.cs`(xUnit)。

```csharp
[Theory]
[MemberData(nameof(GetFixtureBlueprints))]
public void RoundTrip_Should_PreserveSemantics(string fixtureName)
{
    var bp1 = LoadFixture(fixtureName);
    var state = GraphDeserializer.FromBlueprint(bp1);
    var bp2 = GraphSerializer.ToBlueprint(state);

    // 💥 不能直接比 JSON 字符串:AutoLayout 会重排位置,Trait Properties 字典顺序也可能变
    AssertSemanticallyEqual(bp1, bp2);   // DataSource / InitialTraits / Features 内容相等,忽略 Position
}
```

Fixtures:sample (13 节点) + TimeShare (50 节点) + 一组边界 case(空蓝图、仅 DataSource、最大 PortBindings)。

---

## B11. ComponentRegistry 作用域 ✅ 已落地

**触发条件**(已满足):MCP server 子项目跟 K线/分时双业务线并存,需要给同一进程内不同业务的同名 Feature(典型:K线 跟 指标 都叫 `LineSeriesFeature`) 提供 namespace 区分。

### 实际方案:alias prefix + 回退兼容

| 入口 | 文件 | 用途 |
|---|---|---|
| `ComponentRegistry.Register<T>(alias)` | [ComponentRegistry.cs](ComponentRegistry.cs) | 单类型登记自定义别名 (历史就有,业务侧可主动加 prefix) |
| `BuiltinRegistration.RegisterAssemblyOf<T>(string namespacePrefix)` | [BuiltinRegistration.cs](BuiltinRegistration.cs) | **新增**:按程序集批量登记,所有 alias 形如 `"prefix:TypeName"` |
| `ComponentRegistry.Resolve(typeName)` 回退 | [ComponentRegistry.cs](ComponentRegistry.cs) | **新增**:精确匹配失败时,若 typeName 含 `':'` 形态,自动回退查无前缀名 |
| `ComponentRegistry.IsRegistered(typeName)` 回退 | 同上 | DryRun 等预检场景跟 Resolve 同语义,避免预检误报"未登记" |

### 业务形态

```csharp
// 业务 A 用 prefix 隔离自家 feature
BuiltinRegistration.RegisterAssemblyOf<KLineDataSource>("kline");
// 业务 B 同程序集另一前缀注册一份,互不覆盖
BuiltinRegistration.RegisterAssemblyOf<KLineDataSource>("indicator");

// 蓝图侧明确引用
new FeatureModel { TypeName = "kline:LineSeriesFeature", ... }
new FeatureModel { TypeName = "indicator:LineSeriesFeature", ... }

// 旧蓝图(写"LineSeriesFeature")仍然能跑 ——
// Resolve 精确匹配失败 → 回退查无前缀名 → 命中框架自带的 RegisterBuiltins 注册项
new FeatureModel { TypeName = "LineSeriesFeature", ... }    // 仍然有效
```

### 关键设计权衡

- **回退方向选择**:决定从"prefix:Name 找不到 → 回退查 Name",而非反过来。原因:框架自带 builtin 用无前缀名注册(`RegisterBuiltins()` 调 `RegisterAssembly` 的 prefix=null 路径),业务侧在自家程序集附 prefix 时不希望污染框架空间。新蓝图带 prefix 是业务自定义意图,旧蓝图不带 prefix 是兼容性保障 —— 回退方向跟"业务专属版优先,框架基类兜底"的语义对齐。
- **不做完全隔离**:没有引入"作用域树 / namespace context"等重型方案 —— alias prefix 已经够用且零侵入。蓝图 JSON 协议无需改动,只多了一个 `':'` 命名约定。

### 测试覆盖

[ComponentRegistryScopeTests](../../../Hevo.Charting.Tests/ComponentRegistryScopeTests.cs) 7 用例:
- prefix 注册 alias 形态正确
- 同程序集两个 prefix 各自独立
- 带 prefix 查询找不到时回退到无前缀
- IsRegistered 跟 Resolve 同语义回退
- 边界:全无 prefix 找不到、prefix 形态但去 prefix 后仍找不到 → 抛
- 边界:`":Name"` 等空 prefix 仍走回退路径(宽松匹配,跟 v1 兼容)

### 实际工作量:0.5d

---

## B12. 不在本方案内的事

明确划线,避免范围漂移:

- **不引入 Roslyn 动态编译**:MCP 路径走数据/蓝图,不走代码生成(详见聊天讨论结论)。
- **不重构 ChartBlueprint 数据模型**:只优化序列化/反射执行层,模型字段不动。
- **不动 GraphState/Node/Edge schema**:编辑器侧的 IR 跟蓝图解耦,本方案只动蓝图侧。
- **不引入 IL.Emit / DynamicMethod**:Expression 树足够,Emit 路径调试难度跟 .NET 5 兼容性都差一档。
- **不重写 SmartActivator 的 5 重 SafeChangeType**:现有兜底链路稳定,§B3 只把"反射调度"换成"编译委托",类型转换逻辑不动。

---

## §K. Trigger 协议 + Handler 注册表(K线 迁移驱动)

**触发条件**(已满足):业务 schema 含 `Workflow.Interval(...).FetchExclusive(...)` 心跳触发器,以及 `OnRequireDataAsync` / `FutureXLabelFormatter` 等业务闭包,纯蓝图无法表达。

### 协议扩展

| 新增类型/字段 | 文件 | 用途 |
|---|---|---|
| [`TriggerModel`](ChartBlueprint.cs) | ChartBlueprint.cs | 声明 `Workflow.Interval(IntervalSeconds).FetchExclusive(Handler)` 心跳触发器 |
| [`ChartBlueprint.Triggers`](ChartBlueprint.cs) | 同上 | `List<TriggerModel>` 字段 |
| [`BlueprintHandlerRegistry`](BlueprintHandlerRegistry.cs) | 新建 | 命名委托表:string handler 名 → `Delegate` |
| [`BlueprintHandlerAttribute`](BlueprintHandlerRegistry.cs) | 同上 | 业务方法贴这个 attribute,被 AutoDiscover 反射扫到自动注册 |
| [`AutoDiscover` / `AutoDiscoverStatic`](BlueprintHandlerRegistry.cs) | 同上 | 一行注册整个 handler 模块,委托类型按方法签名自动推断 |
| `DynamicChartSchema.WireTrigger` | ChartBlueprint.cs | 把 TriggerModel 复刻成 Workflow 调用链 |
| `DynamicChartSchema.ResolveHandlerReferences` | ChartBlueprint.cs | Feature Properties 里 string 值指向 Delegate 类型属性时,翻译为已注册委托 |
| `BlueprintLauncher.LaunchEx(..., handlers)` | GraphViewer/BlueprintLauncher.cs | 入口加 handlers 参数;DryRun 同步加诊断 (BP_HANDLER_NOT_REGISTERED / BP_TRIGGER_HANDLER_MISSING) |
| `BlueprintRunner.Run(..., handlers)` | BlueprintRunner.cs | 入口加 handlers 参数 |

### 业务侧落地形态

**3 个文件 / 业务**(以 K线 为例,见 [KLineBlueprintModule.cs](../../../Hevo.Drawing/Kline/KLineBlueprintModule.cs)):

```csharp
// 1. PipelinePolicy:数据源管线适配器(只为 SoA 列流这种特殊形态做)
public class KLineSoAPipelinePolicy : PipelinePolicy<KLineDataSource, KLineDataBlock> { ... }

// 2. Handler 模块:业务闭包,[BlueprintHandler] 装饰
public sealed class KLineHandlers
{
    public KLineHandlers(KLineDataSource ds) { _ds = ds; }

    [BlueprintHandler("on_heartbeat")]
    public Task<bool> OnHeartbeatAsync(VersionToken tick, CancellationToken token) { ... }
    [BlueprintHandler("on_require_paging_data")]
    public Task<bool> OnRequirePagingDataAsync(int anchor, int count) { ... }
    [BlueprintHandler("format_future_time")]
    public string FormatFutureTimeLabel(int logicalIndex) { ... }
}

// 3. 蓝图工厂:纯数据,可 JSON 化
public static class KLineBlueprintFactory
{
    public static ChartBlueprint Build() => new ChartBlueprint
    {
        DataSource = ...,
        Triggers = { new TriggerModel { Kind="Interval", IntervalSeconds=1, Handler="on_heartbeat" } },
        Features =
        {
            new FeatureModel { TypeName=nameof(DataPagingFeature),
                Properties = { ["OnRequireDataAsync"] = "on_require_paging_data" } },  // ← Delegate 名引用
            new FeatureModel { TypeName="CrosshairDoubleFeature",
                Properties = { ["FutureXLabelFormatter"] = "format_future_time" } },
            ...
        },
    };
}
```

**Window 装配 3 行**:

```csharp
KLineSoAPipelinePolicy.Register();   // 启动一次
var ds = new KLineDataSource();
var handlers = new BlueprintHandlerRegistry().AutoDiscover(new KLineHandlers(ds));
BlueprintRunner.Run(chartCell, KLineBlueprintFactory.Build(), ds, handlers);
```

### 多业务模块组合

不同业务图各自一个 `*Handlers` 类,registry 串起来:

```csharp
var registry = new BlueprintHandlerRegistry()
    .AutoDiscover(new KLineHandlers(ds))            // K线主图
    .AutoDiscover(new MAIndicatorHandlers(ds))      // MA 指标
    .AutoDiscover(new VolumePanelHandlers(ds));     // 成交量副图
```

handler 名字冲突时按"每 chart cell 一份 registry"隔离即可,简单粗暴。

### 测试覆盖

- [BlueprintAutoDiscoverTests](../../../Hevo.Charting.Tests/BlueprintAutoDiscoverTests.cs) 7 用例:attribute 扫描 / 委托类型推断 / 静态方法 / fluent chain
- [BlueprintTriggerIntegrationTests](../../../Hevo.Charting.Tests/BlueprintTriggerIntegrationTests.cs) 4 用例:K线-shaped 蓝图 DryRun + handler 缺失诊断

### 划线:不做的事

- **不动原 KLineSchema.cs**:平行存在,业务侧二选一
- **不引入 Python / Roslyn 脚本**:命名委托 + AutoDiscover 已让 ergonomics 跟脚本路径接近,且保留 IDE 类型/断点/热重载红利
- **不暴露 BlueprintHandlerRegistry 给 JSON 序列化**:registry 是运行时桥接,蓝图侧只持 string 名字

### §K-2: HandlerKey 强类型注册路径

**触发条件**:主线 handler(典型: K 线 心跳 / 分页)想要"签名错配编译期就拒"的硬保障,而不是等运行时 `IsAssignableFrom` 失败 silent skip 才发现。

**协议增强**(已落地):

| 类型 | 文件 | 用途 |
|---|---|---|
| `HandlerKey<TDelegate>` | [BlueprintHandlerRegistry.cs](BlueprintHandlerRegistry.cs) | zero-cost 值类型,绑"name + 期望委托签名" |
| `BlueprintHandlerRegistryStrongTypedExtensions.Register<T>` / `Get<T>` | 同上 | 注册时签名错配编译期 CS1503;查询直接拿强类型委托 |
| [`WellKnownHandlers`](WellKnownHandlers.cs) | 新建 | 框架内置 Trigger / Paging / Crosshair / Interaction 几类的 HandlerKey 工厂 |

**两条路径并存 (弱 vs 强)**:

```csharp
// 弱类型:AutoDiscover 反射推断,签名错配 ResolveHandlerReferences 运行时 silent skip
var handlers = new BlueprintHandlerRegistry().AutoDiscover(new KLineHandlers(ds));

// 强类型:WellKnownHandlers 工厂出 HandlerKey<T>,Register/Get 编译期 type-check
var h = new KLineHandlers(ds);
var handlers = new BlueprintHandlerRegistry()
    .Register(WellKnownHandlers.Trigger.Fetch("on_heartbeat"),                  h.OnHeartbeatAsync)
    .Register(WellKnownHandlers.Paging.OnRequireDataAsync("on_paging"),         h.OnRequirePagingDataAsync)
    .Register(WellKnownHandlers.Crosshair.FutureXLabel("format_future_time"),   h.FormatFutureTimeLabel);
```

**强弱对比**:

| 维度 | AutoDiscover (弱) | HandlerKey (强) |
|---|---|---|
| Handler 方法签名错配 | 运行时 silent skip → 黑屏 | **编译期 CS1503** |
| Handler 名字打错 | DryRun BP_HANDLER_NOT_REGISTERED 警告 | DryRun 同 (蓝图 JSON 边界仍然 string,无法编译期捕获) |
| 注册代码行数 | 1 行扫整模块 | 每个 handler 一行 |
| IDE 重命名 / Find Refs | ✅ method 名走 attribute string | ⚠️ HandlerKey name 仍 string |
| 学习成本 | 0 | 加一层 WellKnownHandlers 概念 |

**何时用哪个**:
- **K线 / 分时这种主线业务图**:推荐 AutoDiscover 默认路径,简洁
- **真正在意"签名错配不能 silent skip"的核心 handler**(例如金融指标算子涉及钱)→ 显式 Register 强类型多一道防线
- **可以混用**:同 registry 既调 AutoDiscover 又调 Register,同 name 后注册覆盖前者

**业务自定义扩展**:业务 Feature 上的 Delegate 属性想搭便车,新建 `BizWellKnownHandlers` 仿写 [WellKnownHandlers.cs](WellKnownHandlers.cs) 加几个工厂方法即可,框架不必动。

**测试覆盖**:[HandlerKeyStrongTypedTests](../../../Hevo.Charting.Tests/HandlerKeyStrongTypedTests.cs) 7 用例 — round-trip / 类型不匹配返 null / 跟 AutoDiscover 共存 / string 隐式转 HandlerKey / TryGetFetch 兼容性。

