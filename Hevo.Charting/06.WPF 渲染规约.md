# Hevo.Charting WPF 渲染规约

> 文档定位:Renderer 子系统在 **WPF 后端**下的运行时规约(`WpfDrawingRenderer`)。跨后端抽象层(`IRenderer<TBuffer, TContext>` / `IRendererProvider`)与"如何接入新 backend"见 [Renderer.md](Renderer.md)。
>
> 涵盖三块:
> - **§1–§8** 像素对齐契约 — `IDrawingSink` 的 WPF 实现 `WpfDrawingRenderer` 与调用方(业务 Layer / Feature)在像素对齐上的责任划分
> - **§14** 当前实现:snap + AA on 顶点对齐 + ChartCell 容器级配置
> - **§15** WPF 后端现状与性能优化路线图(批量 / 缓存 / 零 GC / 不实施项决策)

---

## 1. 背景与设计目标

### 1.1 单一渲染后端

仓库历史曾并行 `SkiaRenderer` + `WpfDrawingRenderer` 两个后端，命令协议两边语义不一致是早期像素对齐 bug 的主要来源。Skia 后端已于 commit `8350acf` 砍除，目前**唯一渲染后端是** [WpfDrawingRenderer](Renderers/DrawingRenderer.cs)（输出到 `DrawingContext`）。本协议下文所有"渲染器内部"特指 WPF 后端。

光栅旁路 [WpfRasterRenderer](Renderers/WpfRasterRenderer.cs) 只 blit `WriteableBitmap`，不参与命令分发，不在本协议范畴。

### 1.2 历史责任错位（已修复，仅作动机记录）

旧协议把"是否对齐到像素"做成调用方的全局开关 `PushPixelSnapping(thickness)`，业务 Layer 同时还要手工 `halfT = t/2` 内缩"避免外围 Clip 切半"，两者叠加在 fill / stroke / 平滑曲线 混合场景下基本不可能正确，出帧普遍 2px 羽化。当前协议把所有像素算术下沉到渲染器内部，业务侧**零像素心智**。

---

## 2. 核心原则

### 原则 1：**默认锐利 + AA on 同时成立**

正交几何（grid / border / wick / 矩形边）的"物理 1px 锐利"由 `vertex snap` + `IsAntialias=true` 共同实现：snap 把 stroke 中心精确放到像素 .5（奇数线宽）或 .0（偶数线宽），AA scanline 在边界给出 100% / 0% 覆盖率，等价于纯 scanline 锐利。斜线段在同一路径下自动获得 AA 平滑边缘。

> 详见 §14：`vertex snap + IsAntialias=true` 是浏览器 / d3 / Chart.js 的业界默认做法，**不是**关 AA。

### 原则 2：**像素对齐对调用方完全透明**

业务 Layer 调用 `draw.DrawLine / DrawRectangle / DrawPolyline` 时传"原始（未偏移）坐标"。`+ 0.5f` / `halfT = t/2` / `MathF.Round` 这种像素算术**禁止出现在 `Hevo.Charting/Layers` 与 `Hevo.Drawing` 业务程序集**，全部下沉到渲染器命令分发循环。

### 原则 3：**批量原语必须保留批量**

snap 在 `DrawRectangles` / `DrawLineSegments` / `DrawPolyline` 等批量 API 的**循环内部**逐顶点完成，**调用次数与原 batch 一致**。绝不为对齐拆调用、绝不退化成 N 次 `DrawRect`。

---

## 3. 数据模型

### 3.1 `HevoPen` 字段

```csharp
public record HevoPen(
    IHevoBrush Brush,
    double Thickness,
    double[]? DashArray = null,
    int LineCap = 0,
    int LineJoin = 0,
    bool IsAntialias = true   // 默认 AA on：1px H/V 由 snap 保证锐利、斜线段平滑
);
```

唯一与"像素呈现"相关的字段是 `IsAntialias`，默认 `true`。极少数"老电脑保护"场景下业务可显式传 `false` 走纯 scanline 锐利路径。

### 3.2 `LineStyle.Create` 仅控插值

```csharp
public static LineStyle Create(IHevoBrush brush, double thickness = 1.0, bool useSpline = false)
    => new LineStyle(new HevoPen(brush, thickness), useSpline);
```

`useSpline` **仅控制 `LineLayer` 是否走 Catmull-Rom 样条插值**，与 AA / 像素对齐完全无关。原协议中将该参数与"snap 与否"耦合的设计已废弃。

### 3.3 已删除的 API

| 旧 API | 状态 |
|--------|------|
| `IDrawingSink.PushPixelSnapping(thickness)` | 已删除（[DrawingExtensions.cs](Renderers/DrawingExtensions.cs)） |
| `DrawOp.PushGuidelineSet` | 已删除（[Primitives.cs](Renderers/Primitives.cs)） |
| `HevoPen.IsCrisp` | 从未落地（用默认 `IsAntialias=true` 即可，见 §14）|
| 业务侧 `halfT = t/2` / `+ 0.5` / `MathF.Round` 像素算术 | 已扫净 |

---

## 4. 渲染器实现规约

### 4.1 共享 helper：`PixelSnap`

[Renderers/PixelSnap.cs](Renderers/PixelSnap.cs)，`internal`，业务侧不可见。

| 函数 | 语义 |
|------|------|
| `HalfPx(thickness)` | `Ceiling(thickness)` 为奇 → `0.5f`，否则 `0f`。**必须用 Ceiling 而非 (int) 截断**：否则 `thickness=0.5` 等亚像素线宽会被算成偶数（实测 grid 羽化的根因）。 |
| `Vertex(v, halfPx)` | `MathF.Round(v) + halfPx`。把任意浮点坐标 round 到像素边界后再加奇偶补偿。 |
| `SnapEndpoints(p1, p2, halfPx, out a, out b)` | **单线段端点 snap，非对称**：H 时平行轴整数 round + 垂直轴 .5 snap；V 对偶；斜线段两轴都 .5。详见 §14.2 — 对称 .5 snap 会让端点列只 50% 覆盖、淡色 grid 看不见。 |
| `InsideStroke(rect, thickness)` | 先把外边界 4 个角 round 到整像素，再 inside-shrink halfT。**round 必须先于 shrink**：layout 的浮点 plotArea 直接 +halfT 会让 stroke 中心落浮点位置 → AA 横跨 2px 羽化。 |

### 4.2 命令分发模板

每个 stroke 类原语都按"取 pen → 算 halfPx → snap 顶点 → 调 dc"展开。模板见 [DrawingRenderer.cs](Renderers/DrawingRenderer.cs)，关键分支：

| 命令 | 分发逻辑 |
|------|---------|
| `DrawLine` | `HalfPx + SnapEndpoints + dc.DrawLine` |
| `DrawLineSegments` | 配对循环内 `SnapEndpoints` 后 `BeginFigure/LineTo` 写入复用 `StreamGeometry`，最终 `dc.DrawGeometry` 一次出 |
| `DrawPolyline` | `HalfPx` + 循环逐顶点 `Vertex(x, h) / Vertex(y, h)` 写入 `StreamGeometry`，`dc.DrawGeometry` 一次出 |
| `DrawRectangle` | fill 走原 rect；stroke 走 `InsideStroke + dc.DrawRectangle(null, pen, sr)` |
| `DrawRectangles` | 循环内逐 rect：fill 原 rect，stroke 走 `InsideStroke`（**不能用 StreamGeometry 多 figure 批量**：WPF 在 figure 间留下幻线，K 线 body 之间会冒红色"趋势线"伪影） |
| `DrawRoundedRectangle` | `InsideStroke + dc.DrawRoundedRectangle`，圆角部分由 AA on 的 stroke 自然平滑 |
| `DrawEllipse` | 圆心 `MathF.Round` + 直接 `dc.DrawEllipse`（dot 直径 ≥ 3 时 .5 偏移视觉不可分辨） |

### 4.3 WPF 端的 AA 控制

`HevoPen.IsAntialias` 字段默认 `true`，**WPF `Pen` 类型本身不支持 paint 级 AA 开关**，全局 AA 由 [ChartCell.cs:223-224](Core/ChartCell.cs:223) 的 `RenderOptions.SetEdgeMode(_drawingCanvas, EdgeMode.Aliased)` 等价物否定（即整个 canvas 走 aliased）。`pen.IsAntialias=false` 在 WPF 路径下被忽略——这与早期 Skia 后端的 per-paint AA 行为不同，留作 §15 Tier 2 优化中"按命令切 EdgeMode"的潜在改进点。

实际效果：WPF 端 `EdgeMode.Aliased` + `vertex snap` 共同保证 H/V 物理 1px 锐利。斜线段（series 折线）在 aliased 路径下肉眼有锯齿，可接受——业务侧若需平滑可在调用前 push 一个临时 `EdgeMode.Unspecified` 的子 visual（当前未提供 API，路线图）。

### 4.4 性能注意

- `HalfPx(thickness)` 应在循环外预算一次（`DrawPolyline` / `DrawRectangles` 内已遵守）
- `Vertex(...)` / `SnapEndpoints(...)` 单点开销 < 1ns、无 allocation
- WPF `StreamGeometry.Open()` 单次构造目前每帧 new；`StreamGeometry` 复用是路线图项（§15）

---

### 4.5 API 表面契约

#### 4.5.1 ✅ 暴露给业务侧的 API

| API | 用途 | 调用方需要关心像素吗 |
|-----|------|--------------------|
| `IDrawingSink.Push(DrawCmd)` | 入队绘图命令 | ❌ |
| `IDrawingSink.DrawLine / DrawRectangle / DrawPolyline / …`（[DrawingExtensions](Renderers/DrawingExtensions.cs)） | 各原语扩展方法 | ❌ |
| `LineStyle.Create(brush, t, useSpline)` | 创建描边样式 | ❌（`useSpline` 仅控插值） |
| `HevoPen` 构造 | 极少直接用 | 🟡 可显式 `IsAntialias=false` 走纯 scanline，默认 `true` 即可 |
| `IDrawingSink.PushClip / PushOpacity / PushTransform` | 状态栈 | ❌ |

#### 4.5.2 ❌ 已删除 API（不能再依赖）

| 旧 API | 替代 |
|--------|------|
| `IDrawingSink.PushPixelSnapping(thickness)` | 无需替代，删除调用即可 |
| `DrawOp.PushGuidelineSet` | 同上 |
| `HevoPen.IsCrisp` | 用默认行为（snap + AA on）即可；从未落地 |
| 业务侧 `halfT / +0.5 / MathF.Round` | 直接传未偏移坐标 |

#### 4.5.3 🔒 internal 实现细节

```csharp
internal static class PixelSnap   // §4.1
internal class WpfDrawingRenderer : IRenderer<DrawingBuffer, DrawingContext>
```

`PixelSnap` 全部成员 `internal`，业务程序集（`Hevo.Drawing`）不可见。

#### 4.5.4 调用方"无感"验证

Grep 扫描 `Hevo.Drawing` + `Hevo.Charting/Layers`：

- ❌ 无 `PushPixelSnapping` / `PushGuidelineSet` 调用
- ❌ 无 `halfT` 局部变量、`+ 0.5f` 像素偏移
- ❌ 无坐标上下文里的 `MathF.Round`
- ✅ 创建 pen 只通过 `LineStyle.Create(...)`

#### 4.5.5 第三方扩展的边界

将来如再加 `IDrawingSink` 实现（如 D2D / GDI+ / SkiaSharp 重新引入）：

- **必须**在自己的命令分发循环内复用 `PixelSnap` 三个函数（升 `[InternalsVisibleTo]` 或本地 copy 6 行）
- **必须**遵守 stroke 顶点 snap + `IsAntialias` 透传契约
- **不得**重新引入"全局 snap 状态栈"或"调用方手动 +0.5"机制

新 renderer 都应通过 §6 的视觉黄金路径。

---

## 5. 业务图层契约

### 5.1 调用方规则

- ✅ 直接调用 `draw.DrawLine / DrawRectangle / DrawPolyline`，传原始（未偏移）坐标
- ✅ 创建 pen 用 `LineStyle.Create(brush, thickness)`；需要 spline 插值再传 `useSpline: true`
- ❌ 不要写 `+ 0.5` / `halfT` / `MathF.Round` 像素偏移
- ❌ 不要 `using PushPixelSnapping`（API 已删除）
- ❌ 不要在层级局部 `dc.PushTransform(translate(.5))` 模拟对齐

发现某种几何在锐利模式下渲染不正确 → **修渲染器**，不要在 Layer 里打补丁。

### 5.2 Series 折线 / 平滑曲线

```csharp
LineStyle.Create(Colors.Red, 2, useSpline: true);   // 折线走 Catmull-Rom 插值
LineStyle.Create(Colors.LightGray, 1);              // grid / border 直线段
```

`useSpline` 仅控制 `LineLayer` 的几何插值（直线段 vs 样条曲线），**与像素对齐 / AA 都无关**。AA 由 `HevoPen.IsAntialias` 决定，默认 `true`。

### 5.3 已知合规图层

业务侧已经无 `PushPixelSnapping` / `halfT` 残留（commit `b60f57e fix 像素对齐` 清理完）：[PlotAreaDecorLayer](Layers/PlotAreaDecorLayer.cs)、[GridLineLayer](Layers/GridLineLayer.cs)、[CandleLayer](Layers/CandleLayer.cs)、[BarLayer](Layers/BarLayer.cs)、[LineLayer](Layers/LineLayer.cs) 都直接传未偏移坐标。

---

## 6. 测试 / 验收

### 6.1 视觉黄金路径

1. **1 / 2 / 3 px 边框**：渲染矩形边框，扫描其上一行 / 一列像素，断言精确覆盖 1 / 2 / 3 列、覆盖率 100%、无半透明（α == 255）
2. **Grid 横线**：在 axis tick 整数像素 Y 上的横线，扫描该行像素颜色全为 pen color，相邻行无 spillover
3. **Bar / Candle 矩形填充 / 边框**：边界像素 100% 覆盖、外侧像素 0% 覆盖
4. **Series 折线**：直线段在 aliased 路径下端点对齐像素；spline 插值走平滑曲线

### 6.2 跨场景一致性

同一根 grid 横线在以下场景视觉应完全一致：
- 单 cell 占满窗口（cell 位置 X=0）
- `UniformGrid Columns="4"` 多 cell 并排（cell 位置可能 X=374.25 等亚像素）
- DPI 100% / 125% / 150%

亚像素 cell 位置由 §13 的 `EdgeMode.Aliased` 防线兜底——把 cell 内的 vector 像素栅格按最近邻贴到屏幕。

无视觉黄金路径自动化测试，目前依赖人工验收。自动化是 §15 路线项。

---

## 7. 实施回顾

P0（§3 / §4 设计 + 业务侧迁移）+ §14（snap + AA on）已落地，本协议大部分条款都从代码事实回溯。剩余路线项见 §15。

---

## 8. FAQ

**Q：为什么不在 `IDrawingSink` 加 `DrawCrispLine` / `DrawCrispRect` 这种显式版本？**
A：会让"对不对齐"成为调用方的选择，但对齐本身在图表里就是默认正确性。"显式版本"等于把每个调用点都变成可能写错的地方。Pen 携带语义 + 渲染器内部 snap 是 "Pit of Success" 设计。

**Q：批量 API（如 `DrawRectangles`）会因为 snap 退化吗？**
A：不会。snap 算术在批量循环的内部、紧贴 `dc.DrawRectangle` 之前完成，循环次数 = 原批量大小。每个 vertex 多一次 `MathF.Round + 加常量`，纳秒级、零分配。

**Q：偶数线宽（如 2px）也走 snap 吗？**
A：走。`HalfPx(2) == 0`，`Vertex` 等价于 `MathF.Round(v)`，把可能的浮点误差 round 到整数像素，对 2px stroke 同样必要。

**Q：透明 / 半透明描边呢？**
A：snap 只动顶点位置，不动颜色 / alpha。半透明描边在锐利路径下边界依然锐利、内部仍是声明的透明度。

**Q：`useSpline: true` 后 series 还需要做点什么吗？**
A：不需要。AA 默认 on，`useSpline` 单纯控制 `LineLayer` 用 Catmull-Rom 还是直线段。两个维度独立。

**Q：WPF 端如何关 AA？**
A：`HevoPen.IsAntialias=false` 在 WPF Pen 上无效（API 限制），整个 chart canvas 由 `RenderOptions.SetEdgeMode(_drawingCanvas, EdgeMode.Aliased)` 全局走 aliased。如需对单段 Layer 切回 AA-on（如平滑 series），路线项见 §15。

---

## 14. 最终方案：snap + AA on（2026-04-30）

### 14.1 设计依据

`vertex snap + IsAntialias=true` 是浏览器 / d3 / Chart.js / SkiaSharp 官方示例画 1px 锐利线的**业界默认做法**。早期一度怀疑 "stroke 算法本身有 ±1 像素抖动" 而尝试 fill rect 替换 stroke 路径，实测下来标准做法已经能给出精确 1px——AA scanline 在 snap 后的 .5 中心两侧各 50% 覆盖,合并到唯一一列像素 100% 覆盖,就是教科书锐利线。

→ **本协议不再走 fill rect 替换 stroke 路径**,所有 stroke 一律 snap + AA on。

### 14.2 H/V 单线的非对称 snap（关键细节）

⚠️ **H/V 直线不能两轴都 .5 snap**，否则在 AA on 路径下端点会羽化。

设 1px 水平线 `(0, 10) → (600, 10)`，对称 .5 snap 后：
- 端点 = `(0.5, 10.5)` 和 `(600.5, 10.5)`
- Skia stroke 几何 X∈`[0.5, 600.5]`，AA scanline 计算各列覆盖率：
  - 列 X=0：50% 覆盖（淡色 grid 在低对比背景下几乎不可见）
  - 列 X=1..599：100% 覆盖
  - 列 X=600：50% 覆盖（plotArea clip 切掉）
- **视觉效果：grid 线左端淡得看不见、右端被 clip 切掉**

正确做法是 H/V 走**非对称 snap**：
- 平行轴（线方向）：整数 round → 端点列像素 100% 覆盖
- 垂直轴（线宽方向）：.5 snap → stroke 中心落像素中心

斜线段两轴都 .5 snap（标准 vertex snap）。`PixelSnap.SnapEndpoints(p1, p2, halfPx, out a, out b)` 封装这个判定：

```csharp
// 水平
if (p1.Y == p2.Y)
{
    float y = MathF.Round(p1.Y) + halfPx;
    a = new HevoPoint(MathF.Round(p1.X), y);     // 平行轴整数
    b = new HevoPoint(MathF.Round(p2.X), y);
}
// 垂直
else if (p1.X == p2.X)
{
    float x = MathF.Round(p1.X) + halfPx;
    a = new HevoPoint(x, MathF.Round(p1.Y));
    b = new HevoPoint(x, MathF.Round(p2.Y));
}
// 斜线段
else
{
    a = new HevoPoint(Vertex(p1.X, halfPx), Vertex(p1.Y, halfPx));
    b = new HevoPoint(Vertex(p2.X, halfPx), Vertex(p2.Y, halfPx));
}
```

`DrawLine` / `DrawLineSegments` 用 `SnapEndpoints` 替代旧的对称 snap；`DrawPolyline` 中各段虽然也独立但首尾相连成一条 path，端点羽化不可见，仍走对称 snap 即可。

`DrawRectangle` / `DrawRectangles` 走 `InsideStroke` + Skia rect stroke：rect outline 是闭合 4 边几何，相邻边的 stroke 在角点 miter join 互补，每个边界像素都能拿到 100% 覆盖，无端点羽化。

### 14.3 标准做法的工作原理

`vertex snap + IsAntialias=true` 的输出：

| 输入 | snap 后的中心 | AA scanline 算法 | 输出像素 |
|------|--------------|----------------|---------|
| 1px 水平线 Y=10.3 | Y=10.5 | Y=10 像素覆盖率 100%；Y=9 / Y=11 覆盖率 0% | **精确 1 像素行**，纯色 |
| 2px 水平线 Y=10.3 | Y=10.0 | Y=9 / Y=10 像素各 100% 覆盖 | **精确 2 像素行**，纯色 |
| 斜线段（45° 1px 粗） | snap 顶点到 .5 | 每个像素按几何覆盖率算 alpha | **平滑 AA 边缘** |
| 1px 垂直线 X=15.7 | X=15.5 | X=15 像素覆盖率 100% | **精确 1 像素列**，纯色 |

H/V 线在 snap 后中心精确落在 .5（奇数线宽）或 .0（偶数线宽），AA scanline 在边界恰好命中整数像素 → coverage 100% / 0% → 视觉上等价于"无 AA 的锐利线"。**斜线段同时享受 AA 平滑**——一条路径同时正确处理两类几何。

### 14.3 实施落地

| 文件 | 改动 |
|------|------|
| [Renderers/Style.cs](Renderers/Style.cs) `HevoPen` | 末尾追加 `bool IsAntialias = true`，默认开 AA |
| [Renderers/PixelSnap.cs](Renderers/PixelSnap.cs) | 删除 `TryOrthoFill` / `BorderFillRects` / `IntThickness`，仅保留 `HalfPx` / `Vertex` / `InsideStroke` |
| [Renderers/SkiaRenderer.cs](Renderers/SkiaRenderer.cs) | `GetStrokePaint`: `paint.IsAntialias = desc.IsAntialias`；dispatch 各分支回归到 `snap + DrawLine` / `snap + DrawPath` 单一 stroke 路径；删 `FillBorder` helper |
| [Renderers/DrawingRenderer.cs](Renderers/DrawingRenderer.cs) | dispatch 同步回归（WPF 端 `BuildPolyline` / `BuildLineSegments` / `BuildRectangles` snap=true 三个 helper 不变）；删 `FillBorder` |

### 14.4 各场景路径

| 场景 | 路径 | 结果 |
|------|------|------|
| Grid 横竖线（1px H/V） | `DrawLine` → snap + stroke + AA | 锐利 1px ✅ |
| 矩形边框（border） | `DrawRectangle` → InsideStroke + stroke + AA | 锐利边框 ✅ |
| Wick 垂直线 | `DrawLineSegments` → snap + stroke + AA | 锐利 1px ✅ |
| Crosshair（H/V 虚线） | `DrawLine` → snap + stroke + AA + DashEffect | **虚线正确呈现 ✅**（fill rect 路径会吞 dashes） |
| Series 折线（斜线段） | `DrawPolyline` → snap + stroke + AA | 平滑 AA 边缘 ✅ |
| Catmull-Rom spline 输出 | `DrawPolyline` → snap + stroke + AA | 平滑 AA 边缘 ✅ |
| 圆角矩形 stroke | `DrawRoundedRectangle` → InsideStroke + stroke + AA | 锐利直边 + 平滑圆角 ✅ |

### 14.5 业务侧契约（不变）

```csharp
LineStyle.Create(Colors.Red, 2);   // 默认 IsAntialias=true，业务零关心
LineStyle.Create(Colors.Red, 2, useSpline: true);   // 平滑曲线
new HevoPen(brush, 1, IsAntialias: false)  // 极少需要：强制纯 scanline 锐利
```

`HevoPen.IsAntialias` 默认 true 保证开箱即得正确视觉；业务侧若有"老电脑保护"等特殊需求可显式 false 走 nearest-pixel scanline。

### 14.6 跨后端

- **Skia (Hardware)**：`paint.IsAntialias = pen.IsAntialias`，per-pen AA 完全生效
- **WPF (Software)**：WPF Pen 不支持 paint 级 AA 开关，由 [ChartCell.cs:136](Core/ChartCell.cs:136) `_drawingCanvas` 的 `EdgeMode.Aliased` 统一控制；`pen.IsAntialias` 在 WPF 路径下被忽略。WPF 路径主要承载 widget / interaction，AA 缺失影响极小

### 14.7 关键不变量

1. **每条 stroke 顶点都过 snap**：保证 1px 在 .5、2px 在 .0
2. **AA on 是默认**：`HevoPen.IsAntialias = true`
3. **fill 路径 AA off**：`GetFillPaint` 仍 `IsAntialias = false`（多边形 / 矩形 fill 在 AA off 下边界严格落在像素，避免边缘羽化）
4. **DashArray 自动正确**：所有 stroke 走原生 stroke 路径，`SKPathEffect.CreateDash` / WPF `DashStyle` 自然生效

### 14.8 给后续维护者的话

- 如果再遇到 1px stroke "羽化" 现象：**第一步检查 `pen.IsAntialias` 是否 true**（默认）、第二步检查上游坐标是否被某个 layer 提前 round 成整数（破坏了 snap 的 .5 对齐）
- 不要再退回 fill rect 替换 stroke 的方案——会让 DashArray、stroke cap / join 等 pen 配置全部失效

### 14.9 ChartCell 容器级配置(父布局亚像素免疫)

ChartCell 对外承诺:业务侧 `new ChartCell { Template = schema }` 后挂任何容器(`UniformGrid` / `Grid *` / `WrapPanel` / 自定义 Panel),无需关心父级 `UseLayoutRounding` / `SnapsToDevicePixels` / 像素对齐。父布局给的位置即使是 X=374.25,cell 内部像素仍按原栅格呈现。

[ChartCell.cs](Core/ChartCell.cs) 内部 4 条防线:

```csharp
UseLayoutRounding = true;                                              // 自身 measure/arrange 落整像素
SnapsToDevicePixels = true;                                            // 子元素边缘 snap
RenderOptions.SetEdgeMode(_drawingCanvas, EdgeMode.Aliased);           // 静态层禁 visual AA
RenderOptions.SetEdgeMode(_overlayCanvas,  EdgeMode.Aliased);          // 交互层禁 visual AA
```

DPI ≠ 100% 时金融图表的优先级是"锐利 > 平滑",`EdgeMode.Aliased` 在任意 DPI 都能保持像素栅格清晰。这个取舍当前没有切回 AA 的开关——若未来某 cell 内需要开 AA(如带文字背景的图标层),可单独 push 一个 `EdgeMode.Unspecified` 的子 visual。

---

## 15. WPF 后端现状与路线图

> 早期协议 §9 / §10 是 Skia 时代的设计图(`RasterCacheGate` / `CommandOptimizer` / `PaintToken` 等抽象层),Skia 后端砍除后那套抽象自然失效。当前 WPF 端通过另一组机制完成等价目标(`BitmapCache` / `LruCache<HevoPen, Pen>` / `CollectionsMarshal.AsSpan` 等)。本节是当前事实 + 未实现项的具体方案。

### 15.1 已落地（代码事实）

| 维度 | 实现位置 | 与旧 §9 / §10 的对应 |
|------|---------|---------------------|
| **静态层栅格缓存** | [ChartCell.cs:131-200](Core/ChartCell.cs:131) `StaticCachePolicy(On / Off / Adaptive)` + `BitmapCache { SnapsToDevicePixels=true, EnableClearType=false }` | 旧 §9.5 的 Skia `SKSurface` 离屏缓存的 WPF 等价；阈值滞回（`StaticCacheEnableThreshold=500` / `Disable=200`）由 `ReportVisibleDataCount(int)` 驱动 |
| **Paint 资源池** | [DrawingRenderer.cs:24-32](Renderers/DrawingRenderer.cs:24) `LruCache<IHevoBrush, Brush>` / `LruCache<HevoPen, Pen>` / `LruCache<IHevoTypeface, Typeface>` / `LruCache<string, Geometry>` / `LruCache<FormattedTextKey, FormattedText>` | 取代旧 §10.2.3 的 PaintToken/PaintRegistry；按 `IHevoBrush` / `HevoPen` 的 record 语义相等做 key，brush 淘汰重建后文本 cache 仍命中 |
| **Last-used 短路** | [DrawingRenderer.cs:37-42](Renderers/DrawingRenderer.cs:37) `_lastBrushDesc / _lastBrushHandle` | hot loop 内连续同 brush/pen 时绕开 LRU 链表挪移；典型场景：axis labels 同色、candle 同色 body 一帧上百根 |
| **命令队列零 boxing 遍历** | [DrawingRenderer.cs](Renderers/DrawingRenderer.cs) `CollectionsMarshal.AsSpan(buffer.Commands)` + `ref readonly var cmd` | 取代旧 §10.2.4 预分配数组 + `_count` 设计；通过 Span 短路 List 索引边界检查与 DrawCmd 值复制 |
| **Dirty-only 渲染** | [ChartCell.cs:108](Core/ChartCell.cs:108) `_dirtyLayerBuffer` 跨帧复用 + `IsDirty` flag | 取代旧 §10.2.4 的 `LayerDirtyToken`；不做 hash 比较，由 Layer 主动标 dirty |
| **DPI 注入** | [RenderProvider.cs](Renderers/RenderProvider.cs) `WpfRenderProvider.UpdateDpi(...)` + ChartCell 的 `Loaded / DpiChanged` 同步 | 跨屏拖动 / 高 DPI 屏 `FormattedText.PixelsPerDip` 不糊 |
| **诊断 overlay (④)** | [DevTools/RenderDiagnostics.cs](DevTools/RenderDiagnostics.cs) + [ChartCell.GetDiagnostics()](Core/ChartCell.cs) + Sample 端 [DiagnosticsOverlay](../Hevo.Drawing/DiagnosticsOverlay.cs) | 旧 §10.7 的 KPI overlay。`FrameCount` / `LastFrame DrawCmd` / paint cache hit rate / formatted text hit rate / render cost(ms) 五项；renderer 累加 + ChartCell `BeginFrame/EndFrame` 收口；`_diag != null` 时插桩,业务无诊断需求时 null + 单次空判断短路。 |
| **命令分发 batch detector (①)** | [DrawingRenderer.cs](Renderers/DrawingRenderer.cs) `Render` 入口 lookahead + `BuildLineBatch` helper | 旧 §10.5 CommandOptimizer 的精简实现。仅针对相邻 `DrawOp.DrawLine` + `ReferenceEquals(Pen)` 合并为单 `dc.DrawGeometry` 多 figure；`run < 2` 时 fallthrough 到原 switch。axis tick / grid 等场景 N→1 次 dc 调用。 |

### 15.2 不实施项 — 决策记录

> ① 命令分发 batch detector 与 ④ DevTools KPI overlay 已落地（见 §15.1 表）。② / ③ / ⑤ 三项均判定"不实施"，理由见下文。后续若 DiagnosticsOverlay 跑出新瓶颈,再开新条目而非回退到这三项。

#### ② StreamGeometry pool（不实施）

**现状**：[DrawingRenderer.cs](Renderers/DrawingRenderer.cs) 的 `BuildPolyline` / `BuildLineSegments` / `BuildRectangles` / `BuildLineBatch` 每次都 `new StreamGeometry()` + `Freeze()`。每帧 5–10 个一次性 Geometry。

**为什么不实施**：

1. **`StreamGeometry` 就是 WPF 设计来一次性 Freeze 的工具**（`SealedFreezable`），内部状态比 `PathGeometry` 紧凑——这是正确选择。
2. **替代方案 `PathGeometry` + `PolyLineSegment` 池反向更慢**：`PolyLineSegment.Points` 是 `PointCollection`（Freezable 子类），每次 `Add` 走 freezable 通知 + change tracking，单点开销显著高于 `StreamGeometryContext.LineTo`。
3. **不在前 3 大 GC 源**：每帧 5–10 个 Geometry 不是热点，DiagnosticsOverlay 量化前不应优化。
4. **真有 alloc 压力时正确方向是复用 `StreamGeometryContext`** 或预生成共享几何，**不是**换底层几何类型。

**结论**：保留 `new StreamGeometry()` + `Freeze()` 的现状。若未来 DiagnosticsOverlay 跑出"Geometry alloc 进入前 3 大 GC 源"再重新评估,且方向应是 context 复用而非 PathGeometry 池。

#### ③ Per-Op AA 切换（不实施）

**问题**：当前 [DrawingRenderer.cs:336-359](Renderers/DrawingRenderer.cs:336) 的 `GetPen` 不读 `desc.IsAntialias`，因为 WPF `Pen` 不支持 paint 级 AA 开关。`HevoPen.IsAntialias=false` 在 WPF 路径下被忽略——这是 [Style.cs:136](Renderers/Style.cs:136) 注释明确承认的。

**方案探索**：要让 per-op AA 真生效，必须改 ChartCell 架构 — 每个不同 `EdgeMode` 的命令子流走独立 `DrawingVisual`，render 时按 EdgeMode 分桶。代价：`DrawingVisual` 数量翻倍、composition tree 复杂、跨 visual 的 z-order 管理变难。

**结论**：**不实施**。当前 `EdgeMode.Aliased` 全局策略 + `vertex snap + AA on stroke`（§14）已覆盖所有场景。`HevoPen.IsAntialias` 字段保留作"未来引入新后端时的契约位"，WPF 端永久忽略并在 `Style.cs` 注释明确这一点。

**唯一例外**：series 折线斜线段在 aliased 路径下肉眼锯齿明显。如果未来真有用户反馈，最小入侵的方案是在 `LineLayer` 内部为 spline 输出的命令子流新建一个独立 `DrawingVisual` 并设 `EdgeMode.Unspecified`，不改全局架构。这是 **opt-in 的 layer 级方案**，不是协议变更。

#### ⑤ Per-Layer raster cache（不实施 — 方针下放业务）

**为什么不做框架级机制**：

1. **现状已有粗粒度方案**：[ChartCell.cs:215](Core/ChartCell.cs:215) `BitmapCache` 挂在 `_drawingCanvas` 上，默认 On + Adaptive 阈值滞回，相当于"per-cell raster cache"。多 cell 场景下系统已隐式做了 per-static-tree raster。
2. **AxisLabel 真实瓶颈不在 layer 重画**：贵在 `FormattedText` 构造，已被 [DrawingRenderer.cs:40](Renderers/DrawingRenderer.cs:40) `LruCache<FormattedTextKey, FormattedText>(4096)` 拦下。再上 `RenderTargetBitmap` 缓存的是已缓存对象的 composite 调用，几乎免费。
3. **`RenderTargetBitmap.Render` 不便宜**：软件渲染管线，每次 invalidate 全量 re-render，命中率 < 70% 净亏。靠 DiagnosticsOverlay 监控命中率反向证明哪些 layer 不该开 — 一开始就别开。
4. **5–7 天工作量 + LRU 池 + 字节预算 + DPI 重建 + 与现有 `BitmapCache` 共存路径**，系统复杂度跃升一档，换的是少数候选 layer 也许快几 ms。
5. **内存陷阱无法绕开**：朴素方案多 cell 场景下 0.5 GB 起步;修订版的 64 MB 池 + LRU + 自动降级本质是放弃信号。

**方针**：业务侧若有"贵 × 静 × 小"的 layer（典型如 Watermark / Logo）出现卡顿，自行在该 layer 的子 `DrawingVisual` 上挂 `CacheMode = new BitmapCache(...)`，5 行代码即可。WPF 原生就支持子 visual 级 BitmapCache，框架不提供 per-layer 缓存机制,也不引入 `RenderMode` 枚举或 `LayerCacheKey` 抽象。

### 15.3 现状一览

| 项 | 状态 | 备注 |
|----|------|------|
| ① 命令分发层 batch detector | ✅ 已落地 | Render lookahead + `BuildLineBatch`,axis / grid N→1 次 dc 调用 |
| ④ DevTools KPI overlay | ✅ 已落地 | DiagnosticsSnapshot / DiagnosticsOverlay |
| ② StreamGeometry pool | ⚫ 不实施 | `StreamGeometry` 已是 WPF 一次性几何的正确工具,替代方案反向更慢 |
| ③ Per-Op AA 切换 | ⚫ 不实施 | 收益 < 架构成本;由 layer 级独立 `DrawingVisual` opt-in 解决 |
| ⑤ Per-Layer raster cache | ⚫ 不实施 | 方针下放业务,使用子 visual `CacheMode` 即可,框架不引入抽象 |

> 整体原则:**先量化、再优化**。后续若 DiagnosticsOverlay 跑出新瓶颈再开新条目,不预先排队"待评估"项。

### 15.4 视觉自动化测试（旧 §6.1 的实施缺口）

测试入口 `Hevo.Drawing.Sample` 当前是手动验收，仓库无 tests 项目（README §3 也确认 build-only 验证）。建议补 `Hevo.Charting.Tests`：

- 用 `RenderTargetBitmap` 离屏渲染一组标准 chart 场景
- 像素采样断言：1/2/3px border 列覆盖率、grid 行像素纯色、bar 边界 100%
- CI 跑这套自动回归

工作量：1-2 天搭建 + 持续维护（黄金图随主题变更要同步）。

